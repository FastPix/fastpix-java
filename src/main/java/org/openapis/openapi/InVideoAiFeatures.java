/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
package org.openapis.openapi;

import static org.openapis.openapi.operations.Operations.RequestOperation;

import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;
import org.openapis.openapi.models.operations.UpdateMediaModerationRequest;
import org.openapis.openapi.models.operations.UpdateMediaModerationRequestBody;
import org.openapis.openapi.models.operations.UpdateMediaModerationRequestBuilder;
import org.openapis.openapi.models.operations.UpdateMediaModerationResponse;
import org.openapis.openapi.models.operations.UpdateMediaNamedEntitiesRequest;
import org.openapis.openapi.models.operations.UpdateMediaNamedEntitiesRequestBody;
import org.openapis.openapi.models.operations.UpdateMediaNamedEntitiesRequestBuilder;
import org.openapis.openapi.models.operations.UpdateMediaNamedEntitiesResponse;
import org.openapis.openapi.operations.UpdateMediaModeration;
import org.openapis.openapi.operations.UpdateMediaNamedEntities;
import org.openapis.openapi.utils.Headers;
import org.openapis.openapi.utils.Options;


public class InVideoAiFeatures {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncInVideoAiFeatures asyncSDK;

    InVideoAiFeatures(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncInVideoAiFeatures(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncInVideoAiFeatures async() {
        return asyncSDK;
    }

    /**
     * Generate named entities
     * 
     * <p>This endpoint allows you to extract named entities from an existing media.
     * Named Entity Recognition (NER) is a fundamental natural language processing (NLP) technique that
     * identifies and classifies key information (entities) in text into predefined categories. For
     * instance:
     * 
     * <p>- Organizations (for example, "Microsoft", "United Nations")
     * - Locations (for example, "Paris", "Mount Everest")
     * - Product names (for example, "iPhone", "Coca-Cola")
     * 
     * <p>#### How it works
     * 1. Make a PATCH request to this endpoint, replacing `&lt;mediaId&gt;` with the ID of the media you
     * want to extract named-entities.
     * 2. Include the `namedEntities` parameter in the request body to enable.
     * 3. Receive a response containing the updated media data, confirming the changes made.
     * 
     * <p>You can use the <a
     * href="https://docs.fastpix.io/docs/ai-events#videomediaainamedentitiesready">video.mediaAI.named-entities.ready</a>
     * webhook event to track and notify about the named entities extraction.
     * 
     * <p>**Use case:** If a user uploads a video and later decides to enable named entity extraction without
     * re-uploading the entire video.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/generate-named-entities">Named entities</a>
     * 
     * @return The call builder
     */
    public UpdateMediaNamedEntitiesRequestBuilder generateNamedEntities() {
        return new UpdateMediaNamedEntitiesRequestBuilder(sdkConfiguration);
    }

    /**
     * Generate named entities
     * 
     * <p>This endpoint allows you to extract named entities from an existing media.
     * Named Entity Recognition (NER) is a fundamental natural language processing (NLP) technique that
     * identifies and classifies key information (entities) in text into predefined categories. For
     * instance:
     * 
     * <p>- Organizations (for example, "Microsoft", "United Nations")
     * - Locations (for example, "Paris", "Mount Everest")
     * - Product names (for example, "iPhone", "Coca-Cola")
     * 
     * <p>#### How it works
     * 1. Make a PATCH request to this endpoint, replacing `&lt;mediaId&gt;` with the ID of the media you
     * want to extract named-entities.
     * 2. Include the `namedEntities` parameter in the request body to enable.
     * 3. Receive a response containing the updated media data, confirming the changes made.
     * 
     * <p>You can use the <a
     * href="https://docs.fastpix.io/docs/ai-events#videomediaainamedentitiesready">video.mediaAI.named-entities.ready</a>
     * webhook event to track and notify about the named entities extraction.
     * 
     * <p>**Use case:** If a user uploads a video and later decides to enable named entity extraction without
     * re-uploading the entire video.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/generate-named-entities">Named entities</a>
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     *         
     * @param body 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateMediaNamedEntitiesResponse generateNamedEntities(@Nonnull String mediaId, @Nonnull UpdateMediaNamedEntitiesRequestBody body) {
        return generateNamedEntities(mediaId, body, null);
    }

    /**
     * Generate named entities
     * 
     * <p>This endpoint allows you to extract named entities from an existing media.
     * Named Entity Recognition (NER) is a fundamental natural language processing (NLP) technique that
     * identifies and classifies key information (entities) in text into predefined categories. For
     * instance:
     * 
     * <p>- Organizations (for example, "Microsoft", "United Nations")
     * - Locations (for example, "Paris", "Mount Everest")
     * - Product names (for example, "iPhone", "Coca-Cola")
     * 
     * <p>#### How it works
     * 1. Make a PATCH request to this endpoint, replacing `&lt;mediaId&gt;` with the ID of the media you
     * want to extract named-entities.
     * 2. Include the `namedEntities` parameter in the request body to enable.
     * 3. Receive a response containing the updated media data, confirming the changes made.
     * 
     * <p>You can use the <a
     * href="https://docs.fastpix.io/docs/ai-events#videomediaainamedentitiesready">video.mediaAI.named-entities.ready</a>
     * webhook event to track and notify about the named entities extraction.
     * 
     * <p>**Use case:** If a user uploads a video and later decides to enable named entity extraction without
     * re-uploading the entire video.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/generate-named-entities">Named entities</a>
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     *         
     * @param body 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateMediaNamedEntitiesResponse generateNamedEntities(
            @Nonnull String mediaId, @Nonnull UpdateMediaNamedEntitiesRequestBody body,
            @Nullable Options options) {
        UpdateMediaNamedEntitiesRequest request = new UpdateMediaNamedEntitiesRequest(mediaId, body);
        RequestOperation<UpdateMediaNamedEntitiesRequest, UpdateMediaNamedEntitiesResponse> operation
              = new UpdateMediaNamedEntities.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Enable video moderation
     * 
     * <p>This endpoint enables moderation features, such as NSFW and profanity filtering, to detect
     * inappropriate content in existing media.
     * 
     * <p>#### How it works
     * 1. Make a `PATCH` request to this endpoint, replacing `&lt;mediaId&gt;` with the ID of the media you
     * want to update.
     * 2. Include the `moderation` object and provide the requried `type` parameter in the request body to
     * specify the media type (for example, video/audio/av).
     * 4. The response contains the updated media data, confirming the changes made.
     * 
     * <p>You can use the <a
     * href="https://docs.fastpix.io/docs/ai-events#videomediaaimoderationready">video.mediaAI.moderation.ready</a>
     * webhook event to track and notify about the detected moderation results.
     * 
     * <p>**Use case:** This is particularly useful when a user uploads a video and later decides to enable
     * moderation detection without the need to re-upload it.
     * 
     * <p>Related guide: <a
     * href="https://docs.fastpix.io/docs/using-nsfw-and-profanity-filter-for-video-moderation">Moderate
     * NSFW &amp; Profanity</a>
     * 
     * @return The call builder
     */
    public UpdateMediaModerationRequestBuilder updateModeration() {
        return new UpdateMediaModerationRequestBuilder(sdkConfiguration);
    }

    /**
     * Enable video moderation
     * 
     * <p>This endpoint enables moderation features, such as NSFW and profanity filtering, to detect
     * inappropriate content in existing media.
     * 
     * <p>#### How it works
     * 1. Make a `PATCH` request to this endpoint, replacing `&lt;mediaId&gt;` with the ID of the media you
     * want to update.
     * 2. Include the `moderation` object and provide the requried `type` parameter in the request body to
     * specify the media type (for example, video/audio/av).
     * 4. The response contains the updated media data, confirming the changes made.
     * 
     * <p>You can use the <a
     * href="https://docs.fastpix.io/docs/ai-events#videomediaaimoderationready">video.mediaAI.moderation.ready</a>
     * webhook event to track and notify about the detected moderation results.
     * 
     * <p>**Use case:** This is particularly useful when a user uploads a video and later decides to enable
     * moderation detection without the need to re-upload it.
     * 
     * <p>Related guide: <a
     * href="https://docs.fastpix.io/docs/using-nsfw-and-profanity-filter-for-video-moderation">Moderate
     * NSFW &amp; Profanity</a>
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     *         
     * @param body 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateMediaModerationResponse updateModeration(@Nonnull String mediaId, @Nonnull UpdateMediaModerationRequestBody body) {
        return updateModeration(mediaId, body, null);
    }

    /**
     * Enable video moderation
     * 
     * <p>This endpoint enables moderation features, such as NSFW and profanity filtering, to detect
     * inappropriate content in existing media.
     * 
     * <p>#### How it works
     * 1. Make a `PATCH` request to this endpoint, replacing `&lt;mediaId&gt;` with the ID of the media you
     * want to update.
     * 2. Include the `moderation` object and provide the requried `type` parameter in the request body to
     * specify the media type (for example, video/audio/av).
     * 4. The response contains the updated media data, confirming the changes made.
     * 
     * <p>You can use the <a
     * href="https://docs.fastpix.io/docs/ai-events#videomediaaimoderationready">video.mediaAI.moderation.ready</a>
     * webhook event to track and notify about the detected moderation results.
     * 
     * <p>**Use case:** This is particularly useful when a user uploads a video and later decides to enable
     * moderation detection without the need to re-upload it.
     * 
     * <p>Related guide: <a
     * href="https://docs.fastpix.io/docs/using-nsfw-and-profanity-filter-for-video-moderation">Moderate
     * NSFW &amp; Profanity</a>
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     *         
     * @param body 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateMediaModerationResponse updateModeration(
            @Nonnull String mediaId, @Nonnull UpdateMediaModerationRequestBody body,
            @Nullable Options options) {
        UpdateMediaModerationRequest request = new UpdateMediaModerationRequest(mediaId, body);
        RequestOperation<UpdateMediaModerationRequest, UpdateMediaModerationResponse> operation
              = new UpdateMediaModeration.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
