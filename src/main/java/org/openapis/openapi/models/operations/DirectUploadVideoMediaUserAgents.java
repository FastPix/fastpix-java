/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
package org.openapis.openapi.models.operations;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nullable;
import java.lang.Override;
import java.lang.String;
import java.util.List;
import java.util.Optional;
import org.openapis.openapi.utils.Utils;


public class DirectUploadVideoMediaUserAgents {
    /**
     * Specifies the default access policy for user agents (browsers, bots, etc.).
     * If set to `allow`, all user agents are allowed access unless otherwise specified in the `deny` list.
     * If set to `deny`, all user agents are denied access unless otherwise specified in the `allow` list.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("defaultPolicy")
    private DirectUploadVideoMediaUserAgentsDefaultPolicy defaultPolicy;

    /**
     * A list of user agents (identified by string names or patterns) that are explicitly allowed access.
     * This list is only effective when the `defaultPolicy` is set to `deny`.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("allow")
    private List<String> allow;

    /**
     * A list of user agents (identified by string names or patterns) that are explicitly denied access.
     * This list is only effective when the `defaultPolicy` is set to `allow`.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("deny")
    private List<String> deny;

    @JsonCreator
    public DirectUploadVideoMediaUserAgents(
            @JsonProperty("defaultPolicy") @Nullable DirectUploadVideoMediaUserAgentsDefaultPolicy defaultPolicy,
            @JsonProperty("allow") @Nullable List<String> allow,
            @JsonProperty("deny") @Nullable List<String> deny) {
        this.defaultPolicy = defaultPolicy;
        this.allow = allow;
        this.deny = deny;
    }
    
    public DirectUploadVideoMediaUserAgents() {
        this(null, null, null);
    }

    /**
     * Specifies the default access policy for user agents (browsers, bots, etc.).
     * If set to `allow`, all user agents are allowed access unless otherwise specified in the `deny` list.
     * If set to `deny`, all user agents are denied access unless otherwise specified in the `allow` list.
     */
    public Optional<DirectUploadVideoMediaUserAgentsDefaultPolicy> defaultPolicy() {
        return Optional.ofNullable(this.defaultPolicy);
    }

    /**
     * A list of user agents (identified by string names or patterns) that are explicitly allowed access.
     * This list is only effective when the `defaultPolicy` is set to `deny`.
     */
    public Optional<List<String>> allow() {
        return Optional.ofNullable(this.allow);
    }

    /**
     * A list of user agents (identified by string names or patterns) that are explicitly denied access.
     * This list is only effective when the `defaultPolicy` is set to `allow`.
     */
    public Optional<List<String>> deny() {
        return Optional.ofNullable(this.deny);
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Specifies the default access policy for user agents (browsers, bots, etc.).
     * If set to `allow`, all user agents are allowed access unless otherwise specified in the `deny` list.
     * If set to `deny`, all user agents are denied access unless otherwise specified in the `allow` list.
     */
    public DirectUploadVideoMediaUserAgents withDefaultPolicy(@Nullable DirectUploadVideoMediaUserAgentsDefaultPolicy defaultPolicy) {
        this.defaultPolicy = defaultPolicy;
        return this;
    }


    /**
     * A list of user agents (identified by string names or patterns) that are explicitly allowed access.
     * This list is only effective when the `defaultPolicy` is set to `deny`.
     */
    public DirectUploadVideoMediaUserAgents withAllow(@Nullable List<String> allow) {
        this.allow = allow;
        return this;
    }


    /**
     * A list of user agents (identified by string names or patterns) that are explicitly denied access.
     * This list is only effective when the `defaultPolicy` is set to `allow`.
     */
    public DirectUploadVideoMediaUserAgents withDeny(@Nullable List<String> deny) {
        this.deny = deny;
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        DirectUploadVideoMediaUserAgents other = (DirectUploadVideoMediaUserAgents) o;
        return 
            Utils.enhancedDeepEquals(this.defaultPolicy, other.defaultPolicy) &&
            Utils.enhancedDeepEquals(this.allow, other.allow) &&
            Utils.enhancedDeepEquals(this.deny, other.deny);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            defaultPolicy, allow, deny);
    }
    
    @Override
    public String toString() {
        return Utils.toString(DirectUploadVideoMediaUserAgents.class,
                "defaultPolicy", defaultPolicy,
                "allow", allow,
                "deny", deny);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private DirectUploadVideoMediaUserAgentsDefaultPolicy defaultPolicy;

        private List<String> allow;

        private List<String> deny;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * Specifies the default access policy for user agents (browsers, bots, etc.).
         * If set to `allow`, all user agents are allowed access unless otherwise specified in the `deny` list.
         * If set to `deny`, all user agents are denied access unless otherwise specified in the `allow` list.
         */
        public Builder defaultPolicy(@Nullable DirectUploadVideoMediaUserAgentsDefaultPolicy defaultPolicy) {
            this.defaultPolicy = defaultPolicy;
            return this;
        }

        /**
         * A list of user agents (identified by string names or patterns) that are explicitly allowed access.
         * This list is only effective when the `defaultPolicy` is set to `deny`.
         */
        public Builder allow(@Nullable List<String> allow) {
            this.allow = allow;
            return this;
        }

        /**
         * A list of user agents (identified by string names or patterns) that are explicitly denied access.
         * This list is only effective when the `defaultPolicy` is set to `allow`.
         */
        public Builder deny(@Nullable List<String> deny) {
            this.deny = deny;
            return this;
        }

        public DirectUploadVideoMediaUserAgents build() {
            return new DirectUploadVideoMediaUserAgents(
                defaultPolicy, allow, deny);
        }

    }
}
