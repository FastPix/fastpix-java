/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
package org.openapis.openapi.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * AudioTrackType
 * 
 * <p>Defines the type of input track.
 */
public class AudioTrackType {

    public static final AudioTrackType AUDIO = new AudioTrackType("audio");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, AudioTrackType> values = createValuesMap();
    private static final Map<String, AudioTrackTypeEnum> enums = createEnumsMap();

    private final String value;

    private AudioTrackType(String value) {
        this.value = value;
    }

    /**
     * Returns a AudioTrackType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as AudioTrackType
     */ 
    @JsonCreator
    public static AudioTrackType of(String value) {
        synchronized (AudioTrackType.class) {
            return values.computeIfAbsent(value, v -> new AudioTrackType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<AudioTrackTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        AudioTrackType other = (AudioTrackType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "AudioTrackType [value=" + value + "]";
    }

    // return an array just like an enum
    public static AudioTrackType[] values() {
        synchronized (AudioTrackType.class) {
            return values.values().toArray(new AudioTrackType[] {});
        }
    }

    private static final Map<String, AudioTrackType> createValuesMap() {
        Map<String, AudioTrackType> map = new LinkedHashMap<>();
        map.put("audio", AUDIO);
        return map;
    }

    private static final Map<String, AudioTrackTypeEnum> createEnumsMap() {
        Map<String, AudioTrackTypeEnum> map = new HashMap<>();
        map.put("audio", AudioTrackTypeEnum.AUDIO);
        return map;
    }
    
    
    public enum AudioTrackTypeEnum {

        AUDIO("audio"),;

        private final String value;

        private AudioTrackTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

