/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
package org.openapis.openapi.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.core.type.TypeReference;
import jakarta.annotation.Nullable;
import java.lang.Long;
import java.lang.Override;
import java.lang.String;
import java.util.List;
import java.util.Optional;
import org.openapis.openapi.utils.LazySingletonValue;
import org.openapis.openapi.utils.Utils;


public class PullVideoInput {
    /**
     * Defines the type of input.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("type")
    private String type;

    /**
     * The URL hosts the media file for FastPix, which needs to be downloaded to use further. It supports
     * formats like MP3, MP4, MOV, MKV, or TS, and includes text tracks for subtitles or closed captions
     * (SRT/VTT files). The URL must be valid, publicly accessible, and downloadable to ensure FastPix can
     * fetch the file successfully.
     * 
     * <p>While FastPix can handle various audio and video formats and codecs, using standard and widely
     * supported formats helps achieve optimal processing speed.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("url")
    private String url;

    /**
     * Start time indicates where encoding must begin within the video file. For example, if you want to
     * encode a segment from 3 minutes (180 seconds) to 6 minutes (360 seconds) in a 10-minute (600
     * seconds) video, the start time is 3 minutes (180 seconds). Note: Start time is always mentioned in
     * seconds.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("startTime")
    private Long startTime;

    /**
     * End time indicates where encoding must end within the video file. For example, if you want to encode
     * a segment from 3 minutes (180 seconds) to 6 minutes (360 seconds) in a 10-minute (600 seconds)
     * video, the end time is 6 minutes (360 seconds). Note: End time is always mentioned in seconds.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("endTime")
    private Long endTime;

    /**
     * The URL of the **intro video** to be added at the beginning of the media file.
     * The URL must be **valid, publicly accessible, and downloadable** so that FastPix can fetch the file
     * successfully.
     * Supported video formats include **MP4, MOV, MKV, and TS** for optimal processing performance.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("introUrl")
    private String introUrl;

    /**
     * The URL of the **outro video** to be added at the end of the media file.
     * The URL must be **valid, publicly accessible, and downloadable** so that FastPix can retrieve the
     * file successfully.
     * Supported video formats include **MP4, MOV, MKV, and TS** for best compatibility and processing
     * speed.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("outroUrl")
    private String outroUrl;

    /**
     * The list of start and end times (in seconds) of the segments to be removed from the actual video.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("expungeSegments")
    private List<String> expungeSegments;

    /**
     * A list of media segments to be added or processed. Each segment includes details such as the URL of
     * the media file and instructions on where it should be inserted in the final media composition. A
     * segment can either specify an exact timestamp  (`insertAt`) or indicate that it must be added at the
     * end (`insertAtEnd`).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("segments")
    private List<PullVideoInputSegmentUnion> segments;

    @JsonCreator
    public PullVideoInput(
            @JsonProperty("type") @Nullable String type,
            @JsonProperty("url") @Nullable String url,
            @JsonProperty("startTime") @Nullable Long startTime,
            @JsonProperty("endTime") @Nullable Long endTime,
            @JsonProperty("introUrl") @Nullable String introUrl,
            @JsonProperty("outroUrl") @Nullable String outroUrl,
            @JsonProperty("expungeSegments") @Nullable List<String> expungeSegments,
            @JsonProperty("segments") @Nullable List<PullVideoInputSegmentUnion> segments) {
        this.type = Optional.ofNullable(type)
            .orElse(Builder._SINGLETON_VALUE_Type.value());
        this.url = Optional.ofNullable(url)
            .orElse(Builder._SINGLETON_VALUE_Url.value());
        this.startTime = startTime;
        this.endTime = endTime;
        this.introUrl = introUrl;
        this.outroUrl = outroUrl;
        this.expungeSegments = expungeSegments;
        this.segments = segments;
    }
    
    public PullVideoInput() {
        this(null, null, null,
            null, null, null,
            null, null);
    }

    /**
     * Defines the type of input.
     */
    public Optional<String> type() {
        return Optional.ofNullable(this.type);
    }

    /**
     * The URL hosts the media file for FastPix, which needs to be downloaded to use further. It supports
     * formats like MP3, MP4, MOV, MKV, or TS, and includes text tracks for subtitles or closed captions
     * (SRT/VTT files). The URL must be valid, publicly accessible, and downloadable to ensure FastPix can
     * fetch the file successfully.
     * 
     * <p>While FastPix can handle various audio and video formats and codecs, using standard and widely
     * supported formats helps achieve optimal processing speed.
     */
    public Optional<String> url() {
        return Optional.ofNullable(this.url);
    }

    /**
     * Start time indicates where encoding must begin within the video file. For example, if you want to
     * encode a segment from 3 minutes (180 seconds) to 6 minutes (360 seconds) in a 10-minute (600
     * seconds) video, the start time is 3 minutes (180 seconds). Note: Start time is always mentioned in
     * seconds.
     */
    public Optional<Long> startTime() {
        return Optional.ofNullable(this.startTime);
    }

    /**
     * End time indicates where encoding must end within the video file. For example, if you want to encode
     * a segment from 3 minutes (180 seconds) to 6 minutes (360 seconds) in a 10-minute (600 seconds)
     * video, the end time is 6 minutes (360 seconds). Note: End time is always mentioned in seconds.
     */
    public Optional<Long> endTime() {
        return Optional.ofNullable(this.endTime);
    }

    /**
     * The URL of the **intro video** to be added at the beginning of the media file.
     * The URL must be **valid, publicly accessible, and downloadable** so that FastPix can fetch the file
     * successfully.
     * Supported video formats include **MP4, MOV, MKV, and TS** for optimal processing performance.
     */
    public Optional<String> introUrl() {
        return Optional.ofNullable(this.introUrl);
    }

    /**
     * The URL of the **outro video** to be added at the end of the media file.
     * The URL must be **valid, publicly accessible, and downloadable** so that FastPix can retrieve the
     * file successfully.
     * Supported video formats include **MP4, MOV, MKV, and TS** for best compatibility and processing
     * speed.
     */
    public Optional<String> outroUrl() {
        return Optional.ofNullable(this.outroUrl);
    }

    /**
     * The list of start and end times (in seconds) of the segments to be removed from the actual video.
     */
    public Optional<List<String>> expungeSegments() {
        return Optional.ofNullable(this.expungeSegments);
    }

    /**
     * A list of media segments to be added or processed. Each segment includes details such as the URL of
     * the media file and instructions on where it should be inserted in the final media composition. A
     * segment can either specify an exact timestamp  (`insertAt`) or indicate that it must be added at the
     * end (`insertAtEnd`).
     */
    public Optional<List<PullVideoInputSegmentUnion>> segments() {
        return Optional.ofNullable(this.segments);
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Defines the type of input.
     */
    public PullVideoInput withType(@Nullable String type) {
        this.type = type;
        return this;
    }


    /**
     * The URL hosts the media file for FastPix, which needs to be downloaded to use further. It supports
     * formats like MP3, MP4, MOV, MKV, or TS, and includes text tracks for subtitles or closed captions
     * (SRT/VTT files). The URL must be valid, publicly accessible, and downloadable to ensure FastPix can
     * fetch the file successfully.
     * 
     * <p>While FastPix can handle various audio and video formats and codecs, using standard and widely
     * supported formats helps achieve optimal processing speed.
     */
    public PullVideoInput withUrl(@Nullable String url) {
        this.url = url;
        return this;
    }


    /**
     * Start time indicates where encoding must begin within the video file. For example, if you want to
     * encode a segment from 3 minutes (180 seconds) to 6 minutes (360 seconds) in a 10-minute (600
     * seconds) video, the start time is 3 minutes (180 seconds). Note: Start time is always mentioned in
     * seconds.
     */
    public PullVideoInput withStartTime(@Nullable Long startTime) {
        this.startTime = startTime;
        return this;
    }


    /**
     * End time indicates where encoding must end within the video file. For example, if you want to encode
     * a segment from 3 minutes (180 seconds) to 6 minutes (360 seconds) in a 10-minute (600 seconds)
     * video, the end time is 6 minutes (360 seconds). Note: End time is always mentioned in seconds.
     */
    public PullVideoInput withEndTime(@Nullable Long endTime) {
        this.endTime = endTime;
        return this;
    }


    /**
     * The URL of the **intro video** to be added at the beginning of the media file.
     * The URL must be **valid, publicly accessible, and downloadable** so that FastPix can fetch the file
     * successfully.
     * Supported video formats include **MP4, MOV, MKV, and TS** for optimal processing performance.
     */
    public PullVideoInput withIntroUrl(@Nullable String introUrl) {
        this.introUrl = introUrl;
        return this;
    }


    /**
     * The URL of the **outro video** to be added at the end of the media file.
     * The URL must be **valid, publicly accessible, and downloadable** so that FastPix can retrieve the
     * file successfully.
     * Supported video formats include **MP4, MOV, MKV, and TS** for best compatibility and processing
     * speed.
     */
    public PullVideoInput withOutroUrl(@Nullable String outroUrl) {
        this.outroUrl = outroUrl;
        return this;
    }


    /**
     * The list of start and end times (in seconds) of the segments to be removed from the actual video.
     */
    public PullVideoInput withExpungeSegments(@Nullable List<String> expungeSegments) {
        this.expungeSegments = expungeSegments;
        return this;
    }


    /**
     * A list of media segments to be added or processed. Each segment includes details such as the URL of
     * the media file and instructions on where it should be inserted in the final media composition. A
     * segment can either specify an exact timestamp  (`insertAt`) or indicate that it must be added at the
     * end (`insertAtEnd`).
     */
    public PullVideoInput withSegments(@Nullable List<PullVideoInputSegmentUnion> segments) {
        this.segments = segments;
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PullVideoInput other = (PullVideoInput) o;
        return 
            Utils.enhancedDeepEquals(this.type, other.type) &&
            Utils.enhancedDeepEquals(this.url, other.url) &&
            Utils.enhancedDeepEquals(this.startTime, other.startTime) &&
            Utils.enhancedDeepEquals(this.endTime, other.endTime) &&
            Utils.enhancedDeepEquals(this.introUrl, other.introUrl) &&
            Utils.enhancedDeepEquals(this.outroUrl, other.outroUrl) &&
            Utils.enhancedDeepEquals(this.expungeSegments, other.expungeSegments) &&
            Utils.enhancedDeepEquals(this.segments, other.segments);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            type, url, startTime,
            endTime, introUrl, outroUrl,
            expungeSegments, segments);
    }
    
    @Override
    public String toString() {
        return Utils.toString(PullVideoInput.class,
                "type", type,
                "url", url,
                "startTime", startTime,
                "endTime", endTime,
                "introUrl", introUrl,
                "outroUrl", outroUrl,
                "expungeSegments", expungeSegments,
                "segments", segments);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String type;

        private String url;

        private Long startTime;

        private Long endTime;

        private String introUrl;

        private String outroUrl;

        private List<String> expungeSegments;

        private List<PullVideoInputSegmentUnion> segments;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * Defines the type of input.
         */
        public Builder type(@Nullable String type) {
            this.type = type;
            return this;
        }

        /**
         * The URL hosts the media file for FastPix, which needs to be downloaded to use further. It supports
         * formats like MP3, MP4, MOV, MKV, or TS, and includes text tracks for subtitles or closed captions
         * (SRT/VTT files). The URL must be valid, publicly accessible, and downloadable to ensure FastPix can
         * fetch the file successfully.
         * 
         * <p>While FastPix can handle various audio and video formats and codecs, using standard and widely
         * supported formats helps achieve optimal processing speed.
         */
        public Builder url(@Nullable String url) {
            this.url = url;
            return this;
        }

        /**
         * Start time indicates where encoding must begin within the video file. For example, if you want to
         * encode a segment from 3 minutes (180 seconds) to 6 minutes (360 seconds) in a 10-minute (600
         * seconds) video, the start time is 3 minutes (180 seconds). Note: Start time is always mentioned in
         * seconds.
         */
        public Builder startTime(@Nullable Long startTime) {
            this.startTime = startTime;
            return this;
        }

        /**
         * End time indicates where encoding must end within the video file. For example, if you want to encode
         * a segment from 3 minutes (180 seconds) to 6 minutes (360 seconds) in a 10-minute (600 seconds)
         * video, the end time is 6 minutes (360 seconds). Note: End time is always mentioned in seconds.
         */
        public Builder endTime(@Nullable Long endTime) {
            this.endTime = endTime;
            return this;
        }

        /**
         * The URL of the **intro video** to be added at the beginning of the media file.
         * The URL must be **valid, publicly accessible, and downloadable** so that FastPix can fetch the file
         * successfully.
         * Supported video formats include **MP4, MOV, MKV, and TS** for optimal processing performance.
         */
        public Builder introUrl(@Nullable String introUrl) {
            this.introUrl = introUrl;
            return this;
        }

        /**
         * The URL of the **outro video** to be added at the end of the media file.
         * The URL must be **valid, publicly accessible, and downloadable** so that FastPix can retrieve the
         * file successfully.
         * Supported video formats include **MP4, MOV, MKV, and TS** for best compatibility and processing
         * speed.
         */
        public Builder outroUrl(@Nullable String outroUrl) {
            this.outroUrl = outroUrl;
            return this;
        }

        /**
         * The list of start and end times (in seconds) of the segments to be removed from the actual video.
         */
        public Builder expungeSegments(@Nullable List<String> expungeSegments) {
            this.expungeSegments = expungeSegments;
            return this;
        }

        /**
         * A list of media segments to be added or processed. Each segment includes details such as the URL of
         * the media file and instructions on where it should be inserted in the final media composition. A
         * segment can either specify an exact timestamp  (`insertAt`) or indicate that it must be added at the
         * end (`insertAtEnd`).
         */
        public Builder segments(@Nullable List<PullVideoInputSegmentUnion> segments) {
            this.segments = segments;
            return this;
        }

        public PullVideoInput build() {
            return new PullVideoInput(
                type, url, startTime,
                endTime, introUrl, outroUrl,
                expungeSegments, segments);
        }


        private static final LazySingletonValue<String> _SINGLETON_VALUE_Type =
                new LazySingletonValue<>(
                        "type",
                        "\"video\"",
                        new TypeReference<String>() {});

        private static final LazySingletonValue<String> _SINGLETON_VALUE_Url =
                new LazySingletonValue<>(
                        "url",
                        "\"https://static.fastpix.io/fp-sample-video.mp4\"",
                        new TypeReference<String>() {});
    }
}
