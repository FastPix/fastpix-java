/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
package org.openapis.openapi.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.core.type.TypeReference;
import jakarta.annotation.Nullable;
import java.lang.Long;
import java.lang.Override;
import java.lang.String;
import java.util.Map;
import java.util.Optional;
import org.openapis.openapi.utils.LazySingletonValue;
import org.openapis.openapi.utils.Utils;


public class PatchLiveStreamRequest {
    /**
     * You can search for videos with specific key value pairs using metadata, when you tag a video in
     * "key":"value"s pairs. Dynamic metadata allows you to define a key that allows any value pair. You
     * can have maximum of 255 characters and upto 10 entries are allowed.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("metadata")
    private Map<String, String> metadata;

    /**
     * In case the software streaming the live, gets disrupted for any reason and gets disconnected from
     * FastPix, the reconnect window defines the duration FastPix waits before automatically terminating
     * the stream. Before starting the stream, you can set the reconnect window time which is up to 1800
     * seconds.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("reconnectWindow")
    private Long reconnectWindow;

    @JsonCreator
    public PatchLiveStreamRequest(
            @JsonProperty("metadata") @Nullable Map<String, String> metadata,
            @JsonProperty("reconnectWindow") @Nullable Long reconnectWindow) {
        this.metadata = metadata;
        this.reconnectWindow = Optional.ofNullable(reconnectWindow)
            .orElse(Builder._SINGLETON_VALUE_ReconnectWindow.value());
    }
    
    public PatchLiveStreamRequest() {
        this(null, null);
    }

    /**
     * You can search for videos with specific key value pairs using metadata, when you tag a video in
     * "key":"value"s pairs. Dynamic metadata allows you to define a key that allows any value pair. You
     * can have maximum of 255 characters and upto 10 entries are allowed.
     */
    public Optional<Map<String, String>> metadata() {
        return Optional.ofNullable(this.metadata);
    }

    /**
     * In case the software streaming the live, gets disrupted for any reason and gets disconnected from
     * FastPix, the reconnect window defines the duration FastPix waits before automatically terminating
     * the stream. Before starting the stream, you can set the reconnect window time which is up to 1800
     * seconds.
     */
    public Optional<Long> reconnectWindow() {
        return Optional.ofNullable(this.reconnectWindow);
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * You can search for videos with specific key value pairs using metadata, when you tag a video in
     * "key":"value"s pairs. Dynamic metadata allows you to define a key that allows any value pair. You
     * can have maximum of 255 characters and upto 10 entries are allowed.
     */
    public PatchLiveStreamRequest withMetadata(@Nullable Map<String, String> metadata) {
        this.metadata = metadata;
        return this;
    }


    /**
     * In case the software streaming the live, gets disrupted for any reason and gets disconnected from
     * FastPix, the reconnect window defines the duration FastPix waits before automatically terminating
     * the stream. Before starting the stream, you can set the reconnect window time which is up to 1800
     * seconds.
     */
    public PatchLiveStreamRequest withReconnectWindow(@Nullable Long reconnectWindow) {
        this.reconnectWindow = reconnectWindow;
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PatchLiveStreamRequest other = (PatchLiveStreamRequest) o;
        return 
            Utils.enhancedDeepEquals(this.metadata, other.metadata) &&
            Utils.enhancedDeepEquals(this.reconnectWindow, other.reconnectWindow);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            metadata, reconnectWindow);
    }
    
    @Override
    public String toString() {
        return Utils.toString(PatchLiveStreamRequest.class,
                "metadata", metadata,
                "reconnectWindow", reconnectWindow);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private Map<String, String> metadata;

        private Long reconnectWindow;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * You can search for videos with specific key value pairs using metadata, when you tag a video in
         * "key":"value"s pairs. Dynamic metadata allows you to define a key that allows any value pair. You
         * can have maximum of 255 characters and upto 10 entries are allowed.
         */
        public Builder metadata(@Nullable Map<String, String> metadata) {
            this.metadata = metadata;
            return this;
        }

        /**
         * In case the software streaming the live, gets disrupted for any reason and gets disconnected from
         * FastPix, the reconnect window defines the duration FastPix waits before automatically terminating
         * the stream. Before starting the stream, you can set the reconnect window time which is up to 1800
         * seconds.
         */
        public Builder reconnectWindow(@Nullable Long reconnectWindow) {
            this.reconnectWindow = reconnectWindow;
            return this;
        }

        public PatchLiveStreamRequest build() {
            return new PatchLiveStreamRequest(
                metadata, reconnectWindow);
        }


        private static final LazySingletonValue<Long> _SINGLETON_VALUE_ReconnectWindow =
                new LazySingletonValue<>(
                        "reconnectWindow",
                        "60",
                        new TypeReference<Long>() {});
    }
}
