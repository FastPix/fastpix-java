/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
package org.openapis.openapi;

import static org.openapis.openapi.operations.Operations.RequestOperation;

import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;
import org.openapis.openapi.models.components.MediaIdsRequest;
import org.openapis.openapi.models.operations.ChangeMediaOrderInPlaylistRequest;
import org.openapis.openapi.models.operations.ChangeMediaOrderInPlaylistRequestBuilder;
import org.openapis.openapi.models.operations.ChangeMediaOrderInPlaylistResponse;
import org.openapis.openapi.models.operations.DeleteMediaFromPlaylistRequest;
import org.openapis.openapi.models.operations.DeleteMediaFromPlaylistRequestBuilder;
import org.openapis.openapi.models.operations.DeleteMediaFromPlaylistResponse;
import org.openapis.openapi.operations.ChangeMediaOrderInPlaylist;
import org.openapis.openapi.operations.DeleteMediaFromPlaylist;
import org.openapis.openapi.utils.Headers;
import org.openapis.openapi.utils.Options;

/**
 * Operations for playlist management
 */
public class Playlist {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncPlaylist asyncSDK;

    Playlist(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncPlaylist(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncPlaylist async() {
        return asyncSDK;
    }

    /**
     * Change media order in a playlist by ID
     * 
     * <p>This endpoint allows you to change the order of media items within a playlist. By passing the
     * complete list of media IDs in the desired sequence, the playlist's play order is updated
     * accordingly.
     * #### How it works
     * 
     * <p>- When a user sends a PUT request to this endpoint with the `playlistId` as path parameter and the
     * reordered list of all media IDs in the request body, FastPix updates the playlist to reflect the new
     * media sequence and returns the updated playlist details.
     * 
     * <p>#### Example
     * An e-learning platform rearranges the "Beginner Python Series" playlist by submitting a reordered
     * list of media IDs. The playlist now follows the new sequence, providing learners with a better
     * structured learning path.
     * 
     * @return The call builder
     */
    public ChangeMediaOrderInPlaylistRequestBuilder updateMediaOrder() {
        return new ChangeMediaOrderInPlaylistRequestBuilder(sdkConfiguration);
    }

    /**
     * Change media order in a playlist by ID
     * 
     * <p>This endpoint allows you to change the order of media items within a playlist. By passing the
     * complete list of media IDs in the desired sequence, the playlist's play order is updated
     * accordingly.
     * #### How it works
     * 
     * <p>- When a user sends a PUT request to this endpoint with the `playlistId` as path parameter and the
     * reordered list of all media IDs in the request body, FastPix updates the playlist to reflect the new
     * media sequence and returns the updated playlist details.
     * 
     * <p>#### Example
     * An e-learning platform rearranges the "Beginner Python Series" playlist by submitting a reordered
     * list of media IDs. The playlist now follows the new sequence, providing learners with a better
     * structured learning path.
     * 
     * @param playlistId The unique id of the playlist you want to perform the operation on.
     * @param body The list of mediaId(s) you want to perform the operation on.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ChangeMediaOrderInPlaylistResponse updateMediaOrder(@Nonnull String playlistId, @Nonnull MediaIdsRequest body) {
        return updateMediaOrder(playlistId, body, null);
    }

    /**
     * Change media order in a playlist by ID
     * 
     * <p>This endpoint allows you to change the order of media items within a playlist. By passing the
     * complete list of media IDs in the desired sequence, the playlist's play order is updated
     * accordingly.
     * #### How it works
     * 
     * <p>- When a user sends a PUT request to this endpoint with the `playlistId` as path parameter and the
     * reordered list of all media IDs in the request body, FastPix updates the playlist to reflect the new
     * media sequence and returns the updated playlist details.
     * 
     * <p>#### Example
     * An e-learning platform rearranges the "Beginner Python Series" playlist by submitting a reordered
     * list of media IDs. The playlist now follows the new sequence, providing learners with a better
     * structured learning path.
     * 
     * @param playlistId The unique id of the playlist you want to perform the operation on.
     * @param body The list of mediaId(s) you want to perform the operation on.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ChangeMediaOrderInPlaylistResponse updateMediaOrder(
            @Nonnull String playlistId, @Nonnull MediaIdsRequest body,
            @Nullable Options options) {
        ChangeMediaOrderInPlaylistRequest request = new ChangeMediaOrderInPlaylistRequest(playlistId, body);
        RequestOperation<ChangeMediaOrderInPlaylistRequest, ChangeMediaOrderInPlaylistResponse> operation
              = new ChangeMediaOrderInPlaylist.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Delete media in a playlist by ID
     * 
     * <p>This endpoint allows you to delete one or more media items from an existing playlist. By passing the
     * media ID(s) in the request, the specified media items are removed from the playlist.
     * #### How it works
     * 
     * <p>- When a user sends a DELETE request to this endpoint with the playlist ID as the path parameter and
     * the media ID(s) to be removed in the request body, FastPix deletes the specified media items from
     * the playlist and returns the updated playlist details.
     * 
     * <p>#### Example
     * An e-learning platform removes outdated video tutorials from the "Beginner Python Series" playlist
     * by specifying their media IDs in the request. The playlist is updated to exclude these items,
     * ensuring learners only access relevant content.
     * 
     * @return The call builder
     */
    public DeleteMediaFromPlaylistRequestBuilder removeMedia() {
        return new DeleteMediaFromPlaylistRequestBuilder(sdkConfiguration);
    }

    /**
     * Delete media in a playlist by ID
     * 
     * <p>This endpoint allows you to delete one or more media items from an existing playlist. By passing the
     * media ID(s) in the request, the specified media items are removed from the playlist.
     * #### How it works
     * 
     * <p>- When a user sends a DELETE request to this endpoint with the playlist ID as the path parameter and
     * the media ID(s) to be removed in the request body, FastPix deletes the specified media items from
     * the playlist and returns the updated playlist details.
     * 
     * <p>#### Example
     * An e-learning platform removes outdated video tutorials from the "Beginner Python Series" playlist
     * by specifying their media IDs in the request. The playlist is updated to exclude these items,
     * ensuring learners only access relevant content.
     * 
     * @param playlistId The unique id of the playlist you want to perform the operation on.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DeleteMediaFromPlaylistResponse removeMedia(@Nonnull String playlistId) {
        return removeMedia(playlistId, null, null);
    }

    /**
     * Delete media in a playlist by ID
     * 
     * <p>This endpoint allows you to delete one or more media items from an existing playlist. By passing the
     * media ID(s) in the request, the specified media items are removed from the playlist.
     * #### How it works
     * 
     * <p>- When a user sends a DELETE request to this endpoint with the playlist ID as the path parameter and
     * the media ID(s) to be removed in the request body, FastPix deletes the specified media items from
     * the playlist and returns the updated playlist details.
     * 
     * <p>#### Example
     * An e-learning platform removes outdated video tutorials from the "Beginner Python Series" playlist
     * by specifying their media IDs in the request. The playlist is updated to exclude these items,
     * ensuring learners only access relevant content.
     * 
     * @param playlistId The unique id of the playlist you want to perform the operation on.
     * @param body The list of mediaId(s) you want to perform the operation on.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DeleteMediaFromPlaylistResponse removeMedia(
            @Nonnull String playlistId, @Nullable MediaIdsRequest body,
            @Nullable Options options) {
        DeleteMediaFromPlaylistRequest request = new DeleteMediaFromPlaylistRequest(playlistId, body);
        RequestOperation<DeleteMediaFromPlaylistRequest, DeleteMediaFromPlaylistResponse> operation
              = new DeleteMediaFromPlaylist.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
