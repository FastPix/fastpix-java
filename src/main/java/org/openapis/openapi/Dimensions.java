/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
package org.openapis.openapi;

import static org.openapis.openapi.operations.Operations.RequestlessOperation;
import static org.openapis.openapi.operations.Operations.RequestOperation;

import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;
import org.openapis.openapi.models.operations.DimensionsId;
import org.openapis.openapi.models.operations.ListDimensionsRequestBuilder;
import org.openapis.openapi.models.operations.ListDimensionsResponse;
import org.openapis.openapi.models.operations.ListFilterValuesForDimensionRequest;
import org.openapis.openapi.models.operations.ListFilterValuesForDimensionRequestBuilder;
import org.openapis.openapi.models.operations.ListFilterValuesForDimensionResponse;
import org.openapis.openapi.models.operations.ListFilterValuesForDimensionTimespan;
import org.openapis.openapi.operations.ListDimensions;
import org.openapis.openapi.operations.ListFilterValuesForDimension;
import org.openapis.openapi.utils.Headers;
import org.openapis.openapi.utils.Options;

/**
 * Operations involving dimensions
 */
public class Dimensions {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncDimensions asyncSDK;

    Dimensions(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncDimensions(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncDimensions async() {
        return asyncSDK;
    }

    /**
     * List the dimensions
     * 
     * <p>Retrieves a list of dimensions that can be used as query parameters across various data endpoints.
     * Each dimension has a unique id that can be used to filter data effectively.
     * 
     * <p>The dimensions retrieved from this endpoint can be used in conjunction with the <a
     * href="https://docs.fastpix.io/reference/list_video_views">list video views</a> and <a
     * href="https://docs.fastpix.io/reference/list_by_top_content">list by top content</a> endpoints to
     * filter results based on specific criteria. For example, you can filter views by `browser_name`,
     * `os_name`, `device_type`, and more.
     * 
     * <p>Related guides: <a href="https://docs.fastpix.io/page/what-video-data-do-we-capture#/">What Video
     * Data do we capture?</a> ,   <a href="https://docs.fastpix.io/docs/user-passable-metadata-1">Use
     * passable dimensions</a>
     * 
     * @return The call builder
     */
    public ListDimensionsRequestBuilder list() {
        return new ListDimensionsRequestBuilder(sdkConfiguration);
    }

    /**
     * List the dimensions
     * 
     * <p>Retrieves a list of dimensions that can be used as query parameters across various data endpoints.
     * Each dimension has a unique id that can be used to filter data effectively.
     * 
     * <p>The dimensions retrieved from this endpoint can be used in conjunction with the <a
     * href="https://docs.fastpix.io/reference/list_video_views">list video views</a> and <a
     * href="https://docs.fastpix.io/reference/list_by_top_content">list by top content</a> endpoints to
     * filter results based on specific criteria. For example, you can filter views by `browser_name`,
     * `os_name`, `device_type`, and more.
     * 
     * <p>Related guides: <a href="https://docs.fastpix.io/page/what-video-data-do-we-capture#/">What Video
     * Data do we capture?</a> ,   <a href="https://docs.fastpix.io/docs/user-passable-metadata-1">Use
     * passable dimensions</a>
     * 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListDimensionsResponse listDirect() {
        return list(null);
    }

    /**
     * List the dimensions
     * 
     * <p>Retrieves a list of dimensions that can be used as query parameters across various data endpoints.
     * Each dimension has a unique id that can be used to filter data effectively.
     * 
     * <p>The dimensions retrieved from this endpoint can be used in conjunction with the <a
     * href="https://docs.fastpix.io/reference/list_video_views">list video views</a> and <a
     * href="https://docs.fastpix.io/reference/list_by_top_content">list by top content</a> endpoints to
     * filter results based on specific criteria. For example, you can filter views by `browser_name`,
     * `os_name`, `device_type`, and more.
     * 
     * <p>Related guides: <a href="https://docs.fastpix.io/page/what-video-data-do-we-capture#/">What Video
     * Data do we capture?</a> ,   <a href="https://docs.fastpix.io/docs/user-passable-metadata-1">Use
     * passable dimensions</a>
     * 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListDimensionsResponse list(@Nullable Options options) {
        RequestlessOperation<ListDimensionsResponse> operation
            = new ListDimensions.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest());
    }

    /**
     * List the filter values for a dimension
     * 
     * <p>This endpoint returns the filter values associated with a specific dimension, along with the total
     * number of video views for each value. For example, it can list all `browser_name` (dimension) and
     * show how many views occurred for all available browsers like Chrome, Safari (filter values).
     * 
     * <p>In order to use the <a href="https://docs.fastpix.io/docs/custom-business-metadata">Custom
     * Dimensions</a>, you must enable them in the dashboard under settings option based on the plan you
     * have opted for.
     * 
     * <p>#### Example
     * 
     * <p>A developer wants to know how their video content performs across different browsers. By calling
     * this endpoint for the `device_type` dimension, they can retrieve a breakdown of video views by each
     * device (for example, Desktop, Mobile, Tablet). This data helps the developer understand where
     * optimizations or troubleshooting is necessary.
     * 
     * <p>Related guide: <a
     * href="https://docs.fastpix.io/docs/understand-dashboard-ui#filters-and-timeframes">Filters and
     * timespan</a>
     * 
     * @return The call builder
     */
    public ListFilterValuesForDimensionRequestBuilder listFilterValues() {
        return new ListFilterValuesForDimensionRequestBuilder(sdkConfiguration);
    }

    /**
     * List the filter values for a dimension
     * 
     * <p>This endpoint returns the filter values associated with a specific dimension, along with the total
     * number of video views for each value. For example, it can list all `browser_name` (dimension) and
     * show how many views occurred for all available browsers like Chrome, Safari (filter values).
     * 
     * <p>In order to use the <a href="https://docs.fastpix.io/docs/custom-business-metadata">Custom
     * Dimensions</a>, you must enable them in the dashboard under settings option based on the plan you
     * have opted for.
     * 
     * <p>#### Example
     * 
     * <p>A developer wants to know how their video content performs across different browsers. By calling
     * this endpoint for the `device_type` dimension, they can retrieve a breakdown of video views by each
     * device (for example, Desktop, Mobile, Tablet). This data helps the developer understand where
     * optimizations or troubleshooting is necessary.
     * 
     * <p>Related guide: <a
     * href="https://docs.fastpix.io/docs/understand-dashboard-ui#filters-and-timeframes">Filters and
     * timespan</a>
     * 
     * @param dimensionsId Pass Dimensions Id
     *         
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListFilterValuesForDimensionResponse listFilterValues(@Nonnull DimensionsId dimensionsId) {
        return listFilterValues(dimensionsId, null, null,
            null);
    }

    /**
     * List the filter values for a dimension
     * 
     * <p>This endpoint returns the filter values associated with a specific dimension, along with the total
     * number of video views for each value. For example, it can list all `browser_name` (dimension) and
     * show how many views occurred for all available browsers like Chrome, Safari (filter values).
     * 
     * <p>In order to use the <a href="https://docs.fastpix.io/docs/custom-business-metadata">Custom
     * Dimensions</a>, you must enable them in the dashboard under settings option based on the plan you
     * have opted for.
     * 
     * <p>#### Example
     * 
     * <p>A developer wants to know how their video content performs across different browsers. By calling
     * this endpoint for the `device_type` dimension, they can retrieve a breakdown of video views by each
     * device (for example, Desktop, Mobile, Tablet). This data helps the developer understand where
     * optimizations or troubleshooting is necessary.
     * 
     * <p>Related guide: <a
     * href="https://docs.fastpix.io/docs/understand-dashboard-ui#filters-and-timeframes">Filters and
     * timespan</a>
     * 
     * @param dimensionsId Pass Dimensions Id
     *         
     * @param timespan This parameter specifies the time span between which the video views list must be retrieved by. You can provide either from and to unix epoch timestamps or time duration. The scope of duration is between 60 minutes to 30 days.
     *         
     *         **Accepted formats are:**
     *         
     *         array of epoch timestamps for example 
     *         `timespan[]=1498867200&amp;timespan[]=1498953600`
     *         
     *         duration string for example  
     *         `timespan[]=24:hours` or `timespan[]=7:days`
     *         
     * @param filterby Pass the dimensions and their corresponding values you want to filter the views by. For excluding the values in the filter we can pass "!" before the filter value. The list of filters can be obtained from list of dimensions endpoint.
     *         Example Values : [ browser_name:Chrome , os_name:macOS , !device_name:Galaxy ]
     *         
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListFilterValuesForDimensionResponse listFilterValues(
            @Nonnull DimensionsId dimensionsId, @Nullable ListFilterValuesForDimensionTimespan timespan,
            @Nullable String filterby, @Nullable Options options) {
        ListFilterValuesForDimensionRequest request = new ListFilterValuesForDimensionRequest(dimensionsId, timespan, filterby);
        RequestOperation<ListFilterValuesForDimensionRequest, ListFilterValuesForDimensionResponse> operation
              = new ListFilterValuesForDimension.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
