/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
package org.openapis.openapi;

import static org.openapis.openapi.operations.Operations.RequestOperation;

import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;
import org.openapis.openapi.models.operations.UpdateMediaSummaryRequest;
import org.openapis.openapi.models.operations.UpdateMediaSummaryRequestBody;
import org.openapis.openapi.models.operations.UpdateMediaSummaryRequestBuilder;
import org.openapis.openapi.models.operations.UpdateMediaSummaryResponse;
import org.openapis.openapi.operations.UpdateMediaSummary;
import org.openapis.openapi.utils.Headers;
import org.openapis.openapi.utils.Options;


public class AiFeatures {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncAiFeatures asyncSDK;

    AiFeatures(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncAiFeatures(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncAiFeatures async() {
        return asyncSDK;
    }

    /**
     * Generate video summary
     * 
     * <p>This endpoint allows you to generate the summary for an existing media.
     * 
     * <p>#### How it works
     * 1. Send a `PATCH` request to this endpoint, replacing `&lt;mediaId&gt;` with the ID of the media you
     * want to summarize.
     * 2. Include the `generate` parameter in the request body.
     * 3. Include the `summaryLength` parameter, specify the desired length of the summary in words (for
     * example, 120 words), this determines how concise or detailed the summary will be. If no specific
     * summary length is provided, the default length will be 100 words.
     * 4. The response includes the updated media data and confirmation of the changes applied.
     * 
     * <p>You can use the <a
     * href="https://docs.fastpix.io/docs/ai-events#videomediaaisummaryready">video.mediaAI.summary.ready</a>
     * webhook event to track and notify about the summary generation.
     * 
     * <p>**Use case**: This is particularly useful when a user uploads a video and later chooses to generate
     * a summary without needing to re-upload the video.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/generate-video-summary">Video summary</a>
     * 
     * @return The call builder
     */
    public UpdateMediaSummaryRequestBuilder updateMediaSummary() {
        return new UpdateMediaSummaryRequestBuilder(sdkConfiguration);
    }

    /**
     * Generate video summary
     * 
     * <p>This endpoint allows you to generate the summary for an existing media.
     * 
     * <p>#### How it works
     * 1. Send a `PATCH` request to this endpoint, replacing `&lt;mediaId&gt;` with the ID of the media you
     * want to summarize.
     * 2. Include the `generate` parameter in the request body.
     * 3. Include the `summaryLength` parameter, specify the desired length of the summary in words (for
     * example, 120 words), this determines how concise or detailed the summary will be. If no specific
     * summary length is provided, the default length will be 100 words.
     * 4. The response includes the updated media data and confirmation of the changes applied.
     * 
     * <p>You can use the <a
     * href="https://docs.fastpix.io/docs/ai-events#videomediaaisummaryready">video.mediaAI.summary.ready</a>
     * webhook event to track and notify about the summary generation.
     * 
     * <p>**Use case**: This is particularly useful when a user uploads a video and later chooses to generate
     * a summary without needing to re-upload the video.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/generate-video-summary">Video summary</a>
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     *         
     * @param body 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateMediaSummaryResponse updateMediaSummary(@Nonnull String mediaId, @Nonnull UpdateMediaSummaryRequestBody body) {
        return updateMediaSummary(mediaId, body, null);
    }

    /**
     * Generate video summary
     * 
     * <p>This endpoint allows you to generate the summary for an existing media.
     * 
     * <p>#### How it works
     * 1. Send a `PATCH` request to this endpoint, replacing `&lt;mediaId&gt;` with the ID of the media you
     * want to summarize.
     * 2. Include the `generate` parameter in the request body.
     * 3. Include the `summaryLength` parameter, specify the desired length of the summary in words (for
     * example, 120 words), this determines how concise or detailed the summary will be. If no specific
     * summary length is provided, the default length will be 100 words.
     * 4. The response includes the updated media data and confirmation of the changes applied.
     * 
     * <p>You can use the <a
     * href="https://docs.fastpix.io/docs/ai-events#videomediaaisummaryready">video.mediaAI.summary.ready</a>
     * webhook event to track and notify about the summary generation.
     * 
     * <p>**Use case**: This is particularly useful when a user uploads a video and later chooses to generate
     * a summary without needing to re-upload the video.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/generate-video-summary">Video summary</a>
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     *         
     * @param body 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateMediaSummaryResponse updateMediaSummary(
            @Nonnull String mediaId, @Nonnull UpdateMediaSummaryRequestBody body,
            @Nullable Options options) {
        UpdateMediaSummaryRequest request = new UpdateMediaSummaryRequest(mediaId, body);
        RequestOperation<UpdateMediaSummaryRequest, UpdateMediaSummaryResponse> operation
              = new UpdateMediaSummary.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
