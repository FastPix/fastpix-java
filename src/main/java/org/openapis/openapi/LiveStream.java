/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
package org.openapis.openapi;

import static org.openapis.openapi.operations.Operations.RequestOperation;

import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;
import org.openapis.openapi.models.operations.CompleteLiveStreamRequest;
import org.openapis.openapi.models.operations.CompleteLiveStreamRequestBuilder;
import org.openapis.openapi.models.operations.CompleteLiveStreamResponse;
import org.openapis.openapi.models.operations.DeleteLiveStreamRequest;
import org.openapis.openapi.models.operations.DeleteLiveStreamRequestBuilder;
import org.openapis.openapi.models.operations.DeleteLiveStreamResponse;
import org.openapis.openapi.models.operations.DisableLiveStreamRequest;
import org.openapis.openapi.models.operations.DisableLiveStreamRequestBuilder;
import org.openapis.openapi.models.operations.DisableLiveStreamResponse;
import org.openapis.openapi.models.operations.EnableLiveStreamRequest;
import org.openapis.openapi.models.operations.EnableLiveStreamRequestBuilder;
import org.openapis.openapi.models.operations.EnableLiveStreamResponse;
import org.openapis.openapi.operations.CompleteLiveStream;
import org.openapis.openapi.operations.DeleteLiveStream;
import org.openapis.openapi.operations.DisableLiveStream;
import org.openapis.openapi.operations.EnableLiveStream;
import org.openapis.openapi.utils.Headers;
import org.openapis.openapi.utils.Options;


public class LiveStream {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncLiveStream asyncSDK;

    LiveStream(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncLiveStream(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncLiveStream async() {
        return asyncSDK;
    }

    /**
     * Delete a stream
     * 
     * <p>Permanently deletes a specified live stream from the workspace. If the stream is active, the encoder
     * is disconnected and ingestion stops immediately. This action is irreversible, and any future
     * playback attempts fail as a result.
     * 
     * <p>Provide the `streamId` in the request to terminate active connections and remove the stream from the
     * workspace. You can further look for <a
     * href="https://docs.fastpix.io/docs/live-events#videolive_streamdeleted">video.live_stream.deleted</a>
     * webhook to notify your system about the status.
     * 
     * <p>#### Example
     * 
     * <p>For an online concert platform, a trial stream was mistakenly made public. The event manager deletes
     * the stream before the concert begins to avoid confusion among viewers.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @return The call builder
     */
    public DeleteLiveStreamRequestBuilder delete() {
        return new DeleteLiveStreamRequestBuilder(sdkConfiguration);
    }

    /**
     * Delete a stream
     * 
     * <p>Permanently deletes a specified live stream from the workspace. If the stream is active, the encoder
     * is disconnected and ingestion stops immediately. This action is irreversible, and any future
     * playback attempts fail as a result.
     * 
     * <p>Provide the `streamId` in the request to terminate active connections and remove the stream from the
     * workspace. You can further look for <a
     * href="https://docs.fastpix.io/docs/live-events#videolive_streamdeleted">video.live_stream.deleted</a>
     * webhook to notify your system about the status.
     * 
     * <p>#### Example
     * 
     * <p>For an online concert platform, a trial stream was mistakenly made public. The event manager deletes
     * the stream before the concert begins to avoid confusion among viewers.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @param streamId Upon creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DeleteLiveStreamResponse delete(@Nonnull String streamId) {
        return delete(streamId, null);
    }

    /**
     * Delete a stream
     * 
     * <p>Permanently deletes a specified live stream from the workspace. If the stream is active, the encoder
     * is disconnected and ingestion stops immediately. This action is irreversible, and any future
     * playback attempts fail as a result.
     * 
     * <p>Provide the `streamId` in the request to terminate active connections and remove the stream from the
     * workspace. You can further look for <a
     * href="https://docs.fastpix.io/docs/live-events#videolive_streamdeleted">video.live_stream.deleted</a>
     * webhook to notify your system about the status.
     * 
     * <p>#### Example
     * 
     * <p>For an online concert platform, a trial stream was mistakenly made public. The event manager deletes
     * the stream before the concert begins to avoid confusion among viewers.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @param streamId Upon creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DeleteLiveStreamResponse delete(@Nonnull String streamId, @Nullable Options options) {
        DeleteLiveStreamRequest request = new DeleteLiveStreamRequest(streamId);
        RequestOperation<DeleteLiveStreamRequest, DeleteLiveStreamResponse> operation
              = new DeleteLiveStream.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Enable a stream
     * 
     * <p>This endpoint allows you to enable a livestream by transitioning its status from `disabled` to
     * `idle`. After it is enabled, the stream becomes available and ready to accept an incoming broadcast
     * from a streaming tool.
     * 
     * <p>Streams on the trial plan cannot be re-enabled if they are in the `disabled` state.
     * 
     * <p>The `livestreamId` must be provided in the path, and the stream must not already be in an enabled
     * state (`idle`, `preparing`, or `active`).
     * 
     * <p>#### Example
     * 
     * <p>A creator disables a livestream to pause it temporarily. Later, they decide to continue the session.
     * By calling this endpoint with the stream's ID, they can re-enable and restart the same livestream.
     * 
     * <p>Related guide <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @return The call builder
     */
    public EnableLiveStreamRequestBuilder enable() {
        return new EnableLiveStreamRequestBuilder(sdkConfiguration);
    }

    /**
     * Enable a stream
     * 
     * <p>This endpoint allows you to enable a livestream by transitioning its status from `disabled` to
     * `idle`. After it is enabled, the stream becomes available and ready to accept an incoming broadcast
     * from a streaming tool.
     * 
     * <p>Streams on the trial plan cannot be re-enabled if they are in the `disabled` state.
     * 
     * <p>The `livestreamId` must be provided in the path, and the stream must not already be in an enabled
     * state (`idle`, `preparing`, or `active`).
     * 
     * <p>#### Example
     * 
     * <p>A creator disables a livestream to pause it temporarily. Later, they decide to continue the session.
     * By calling this endpoint with the stream's ID, they can re-enable and restart the same livestream.
     * 
     * <p>Related guide <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @param streamId Upon creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public EnableLiveStreamResponse enable(@Nonnull String streamId) {
        return enable(streamId, null);
    }

    /**
     * Enable a stream
     * 
     * <p>This endpoint allows you to enable a livestream by transitioning its status from `disabled` to
     * `idle`. After it is enabled, the stream becomes available and ready to accept an incoming broadcast
     * from a streaming tool.
     * 
     * <p>Streams on the trial plan cannot be re-enabled if they are in the `disabled` state.
     * 
     * <p>The `livestreamId` must be provided in the path, and the stream must not already be in an enabled
     * state (`idle`, `preparing`, or `active`).
     * 
     * <p>#### Example
     * 
     * <p>A creator disables a livestream to pause it temporarily. Later, they decide to continue the session.
     * By calling this endpoint with the stream's ID, they can re-enable and restart the same livestream.
     * 
     * <p>Related guide <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @param streamId Upon creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public EnableLiveStreamResponse enable(@Nonnull String streamId, @Nullable Options options) {
        EnableLiveStreamRequest request = new EnableLiveStreamRequest(streamId);
        RequestOperation<EnableLiveStreamRequest, EnableLiveStreamResponse> operation
              = new EnableLiveStream.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Disable a stream
     * 
     * <p>This endpoint disables a livestream by setting its status to `disabled`. Use this to stop a
     * livestream when it's no longer needed or must be taken offline intentionally.
     * 
     * <p>A disabled stream can later be re-enabled using the enable endpoint — however, if you're on a trial
     * plan, re-enabling is not allowed once the stream is disabled.
     * 
     * <p>#### Example
     * 
     * <p>A speaker finishes their live session and wants to prevent the stream from being mistakenly started
     * again. By calling this endpoint, the stream is transitioned to a `disabled` state, ensuring it's
     * permanently stopped (unless re-enabled on a paid plan).
     * 
     * <p>Related guide <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @return The call builder
     */
    public DisableLiveStreamRequestBuilder disable() {
        return new DisableLiveStreamRequestBuilder(sdkConfiguration);
    }

    /**
     * Disable a stream
     * 
     * <p>This endpoint disables a livestream by setting its status to `disabled`. Use this to stop a
     * livestream when it's no longer needed or must be taken offline intentionally.
     * 
     * <p>A disabled stream can later be re-enabled using the enable endpoint — however, if you're on a trial
     * plan, re-enabling is not allowed once the stream is disabled.
     * 
     * <p>#### Example
     * 
     * <p>A speaker finishes their live session and wants to prevent the stream from being mistakenly started
     * again. By calling this endpoint, the stream is transitioned to a `disabled` state, ensuring it's
     * permanently stopped (unless re-enabled on a paid plan).
     * 
     * <p>Related guide <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @param streamId After creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DisableLiveStreamResponse disable(@Nonnull String streamId) {
        return disable(streamId, null);
    }

    /**
     * Disable a stream
     * 
     * <p>This endpoint disables a livestream by setting its status to `disabled`. Use this to stop a
     * livestream when it's no longer needed or must be taken offline intentionally.
     * 
     * <p>A disabled stream can later be re-enabled using the enable endpoint — however, if you're on a trial
     * plan, re-enabling is not allowed once the stream is disabled.
     * 
     * <p>#### Example
     * 
     * <p>A speaker finishes their live session and wants to prevent the stream from being mistakenly started
     * again. By calling this endpoint, the stream is transitioned to a `disabled` state, ensuring it's
     * permanently stopped (unless re-enabled on a paid plan).
     * 
     * <p>Related guide <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @param streamId After creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DisableLiveStreamResponse disable(@Nonnull String streamId, @Nullable Options options) {
        DisableLiveStreamRequest request = new DisableLiveStreamRequest(streamId);
        RequestOperation<DisableLiveStreamRequest, DisableLiveStreamResponse> operation
              = new DisableLiveStream.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Complete a stream
     * 
     * <p>This endpoint marks a livestream as completed by stopping the active stream and transitioning its
     * status to `idle`. It is typically used after a livestream session has ended.
     * 
     * <p>This operation only works when the stream is in the `active` state.
     * 
     * <p>Completing a stream can help finalize the session and trigger post-processing events like VOD
     * generation.
     * 
     * <p>#### Example
     * 
     * <p>A virtual event ends, and the system or host needs to close the livestream to prevent further
     * streaming. This endpoint ensures the livestream status is changed from `active` to `idle`,
     * indicating it's officially completed.
     * 
     * <p>Related guide <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @return The call builder
     */
    public CompleteLiveStreamRequestBuilder complete() {
        return new CompleteLiveStreamRequestBuilder(sdkConfiguration);
    }

    /**
     * Complete a stream
     * 
     * <p>This endpoint marks a livestream as completed by stopping the active stream and transitioning its
     * status to `idle`. It is typically used after a livestream session has ended.
     * 
     * <p>This operation only works when the stream is in the `active` state.
     * 
     * <p>Completing a stream can help finalize the session and trigger post-processing events like VOD
     * generation.
     * 
     * <p>#### Example
     * 
     * <p>A virtual event ends, and the system or host needs to close the livestream to prevent further
     * streaming. This endpoint ensures the livestream status is changed from `active` to `idle`,
     * indicating it's officially completed.
     * 
     * <p>Related guide <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @param streamId Upon creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CompleteLiveStreamResponse complete(@Nonnull String streamId) {
        return complete(streamId, null);
    }

    /**
     * Complete a stream
     * 
     * <p>This endpoint marks a livestream as completed by stopping the active stream and transitioning its
     * status to `idle`. It is typically used after a livestream session has ended.
     * 
     * <p>This operation only works when the stream is in the `active` state.
     * 
     * <p>Completing a stream can help finalize the session and trigger post-processing events like VOD
     * generation.
     * 
     * <p>#### Example
     * 
     * <p>A virtual event ends, and the system or host needs to close the livestream to prevent further
     * streaming. This endpoint ensures the livestream status is changed from `active` to `idle`,
     * indicating it's officially completed.
     * 
     * <p>Related guide <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @param streamId Upon creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CompleteLiveStreamResponse complete(@Nonnull String streamId, @Nullable Options options) {
        CompleteLiveStreamRequest request = new CompleteLiveStreamRequest(streamId);
        RequestOperation<CompleteLiveStreamRequest, CompleteLiveStreamResponse> operation
              = new CompleteLiveStream.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
