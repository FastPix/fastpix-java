/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
package org.openapis.openapi;

import static org.openapis.openapi.operations.Operations.AsyncRequestOperation;

import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.Long;
import java.lang.String;
import java.util.concurrent.CompletableFuture;
import org.openapis.openapi.models.components.SortOrder;
import org.openapis.openapi.models.components.UpdateTrackRequest;
import org.openapis.openapi.models.operations.CancelUploadRequest;
import org.openapis.openapi.models.operations.DeleteMediaRequest;
import org.openapis.openapi.models.operations.GetMediaClipsRequest;
import org.openapis.openapi.models.operations.ListLiveClipsRequest;
import org.openapis.openapi.models.operations.ListUploadsRequest;
import org.openapis.openapi.models.operations.UpdateMediaChaptersRequest;
import org.openapis.openapi.models.operations.UpdateMediaChaptersRequestBody;
import org.openapis.openapi.models.operations.UpdateMediaTrackRequest;
import org.openapis.openapi.models.operations.UpdatedMediaRequest;
import org.openapis.openapi.models.operations.UpdatedMediaRequestBody;
import org.openapis.openapi.models.operations.UpdatedMp4SupportRequest;
import org.openapis.openapi.models.operations.UpdatedMp4SupportRequestBody;
import org.openapis.openapi.models.operations.async.CancelUploadRequestBuilder;
import org.openapis.openapi.models.operations.async.CancelUploadResponse;
import org.openapis.openapi.models.operations.async.DeleteMediaRequestBuilder;
import org.openapis.openapi.models.operations.async.DeleteMediaResponse;
import org.openapis.openapi.models.operations.async.GetMediaClipsRequestBuilder;
import org.openapis.openapi.models.operations.async.GetMediaClipsResponse;
import org.openapis.openapi.models.operations.async.ListLiveClipsRequestBuilder;
import org.openapis.openapi.models.operations.async.ListLiveClipsResponse;
import org.openapis.openapi.models.operations.async.ListUploadsRequestBuilder;
import org.openapis.openapi.models.operations.async.ListUploadsResponse;
import org.openapis.openapi.models.operations.async.UpdateMediaChaptersRequestBuilder;
import org.openapis.openapi.models.operations.async.UpdateMediaChaptersResponse;
import org.openapis.openapi.models.operations.async.UpdateMediaTrackRequestBuilder;
import org.openapis.openapi.models.operations.async.UpdateMediaTrackResponse;
import org.openapis.openapi.models.operations.async.UpdatedMediaRequestBuilder;
import org.openapis.openapi.models.operations.async.UpdatedMediaResponse;
import org.openapis.openapi.models.operations.async.UpdatedMp4SupportRequestBuilder;
import org.openapis.openapi.models.operations.async.UpdatedMp4SupportResponse;
import org.openapis.openapi.operations.CancelUpload;
import org.openapis.openapi.operations.DeleteMedia;
import org.openapis.openapi.operations.GetMediaClips;
import org.openapis.openapi.operations.ListLiveClips;
import org.openapis.openapi.operations.ListUploads;
import org.openapis.openapi.operations.UpdateMediaChapters;
import org.openapis.openapi.operations.UpdateMediaTrack;
import org.openapis.openapi.operations.UpdatedMedia;
import org.openapis.openapi.operations.UpdatedMp4Support;
import org.openapis.openapi.utils.Headers;
import org.openapis.openapi.utils.Options;


public class AsyncVideos {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final Videos syncSDK;

    AsyncVideos(Videos syncSDK, SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.syncSDK = syncSDK;
    }

    /**
     * Switches to the sync SDK.
     * 
     * @return The sync SDK
     */
    public Videos sync() {
        return syncSDK;
    }


    /**
     * Get all clips of a live stream
     * 
     * <p>Retrieves a list of all media clips generated from a specific livestream. Each media entry includes
     * metadata such as the clip media IDs, and other relevant details. A media clip is a segmented portion
     * of an original media file (source live stream).
     * 
     * <p>Clips are often created for various purposes such as previews, highlights, or customized edits.
     * #### How it works
     * 1. Provide the livestreamId as a parameter when calling this endpoint.
     * 
     * <p>2. The API returns a paginated list of media clips created from the specified livestream.
     * 
     * <p>3. Pagination helps maintain performance and usability when handling large sets of media files,
     * making it easier to organize and manage content in bulk.
     * 
     * <p>#### Use case
     * Suppose you’re hosting a live gaming event and want to showcase key moments from the stream — such
     * as top plays or final match highlights. You can use this endpoint to fetch all clips generated from
     * that livestream, display them in your dashboard, or use them for post-event editing and sharing.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/instant-live-clipping">Instant live
     * clipping</a>
     * 
     * @return The async call builder
     */
    public ListLiveClipsRequestBuilder listLiveClips() {
        return new ListLiveClipsRequestBuilder(sdkConfiguration);
    }

    /**
     * Get all clips of a live stream
     * 
     * <p>Retrieves a list of all media clips generated from a specific livestream. Each media entry includes
     * metadata such as the clip media IDs, and other relevant details. A media clip is a segmented portion
     * of an original media file (source live stream).
     * 
     * <p>Clips are often created for various purposes such as previews, highlights, or customized edits.
     * #### How it works
     * 1. Provide the livestreamId as a parameter when calling this endpoint.
     * 
     * <p>2. The API returns a paginated list of media clips created from the specified livestream.
     * 
     * <p>3. Pagination helps maintain performance and usability when handling large sets of media files,
     * making it easier to organize and manage content in bulk.
     * 
     * <p>#### Use case
     * Suppose you’re hosting a live gaming event and want to showcase key moments from the stream — such
     * as top plays or final match highlights. You can use this endpoint to fetch all clips generated from
     * that livestream, display them in your dashboard, or use them for post-event editing and sharing.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/instant-live-clipping">Instant live
     * clipping</a>
     * 
     * @param livestreamId The stream Id is unique identifier assigned to the live stream.
     * @return {@code CompletableFuture<ListLiveClipsResponse>} - The async response
     */
    public CompletableFuture<ListLiveClipsResponse> listLiveClips(@Nonnull String livestreamId) {
        return listLiveClips(
                livestreamId, null, null,
                null, null);
    }

    /**
     * Get all clips of a live stream
     * 
     * <p>Retrieves a list of all media clips generated from a specific livestream. Each media entry includes
     * metadata such as the clip media IDs, and other relevant details. A media clip is a segmented portion
     * of an original media file (source live stream).
     * 
     * <p>Clips are often created for various purposes such as previews, highlights, or customized edits.
     * #### How it works
     * 1. Provide the livestreamId as a parameter when calling this endpoint.
     * 
     * <p>2. The API returns a paginated list of media clips created from the specified livestream.
     * 
     * <p>3. Pagination helps maintain performance and usability when handling large sets of media files,
     * making it easier to organize and manage content in bulk.
     * 
     * <p>#### Use case
     * Suppose you’re hosting a live gaming event and want to showcase key moments from the stream — such
     * as top plays or final match highlights. You can use this endpoint to fetch all clips generated from
     * that livestream, display them in your dashboard, or use them for post-event editing and sharing.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/instant-live-clipping">Instant live
     * clipping</a>
     * 
     * @param livestreamId The stream Id is unique identifier assigned to the live stream.
     * @param limit Limit specifies the maximum number of items to display per page.
     * @param offset Offset determines the starting point for data retrieval within a paginated list.
     * @param orderBy The values in the list can be arranged in two ways: DESC (Descending) or ASC (Ascending).
     * @param options additional options
     * @return {@code CompletableFuture<ListLiveClipsResponse>} - The async response
     */
    public CompletableFuture<ListLiveClipsResponse> listLiveClips(
            @Nonnull String livestreamId, @Nullable Long limit,
            @Nullable Long offset, @Nullable SortOrder orderBy,
            @Nullable Options options) {
        ListLiveClipsRequest request = new ListLiveClipsRequest(
                livestreamId, limit, offset,
                orderBy);
        AsyncRequestOperation<ListLiveClipsRequest, ListLiveClipsResponse> operation
              = new ListLiveClips.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Update a media by ID
     * 
     * <p>This endpoint allows you to update specific parameters of an existing media file. You can modify the
     * key-value pairs of the metadata that were provided in the payload during the creation of media from
     * a URL or when uploading the media directly from device.
     * 
     * <p>#### How it works
     * 
     * <p>1. Make a PATCH request to this endpoint. Replace `&lt;mediaId&gt;` with the unique ID (`uploadId`
     * or `id`) of the media you received after uploading to FastPix
     * 
     * <p>2. Include the updated parameters in the request body.
     * 
     * <p>3. The response returns the updated media data, confirming the changes.
     * 
     * <p>4. Monitor the <a
     * href="https://docs.fastpix.io/docs/media-events#videomediaupdated">video.media.updated</a> webhook
     * event to track the update status in your system.
     * 
     * <p>#### Example
     * If a user uploads a video and later needs to change the title, add a new description, or update
     * tags, you can use this endpoint to update the media metadata without re-uploading the entire video.
     * 
     * @return The async call builder
     */
    public UpdatedMediaRequestBuilder updateMedia() {
        return new UpdatedMediaRequestBuilder(sdkConfiguration);
    }

    /**
     * Update a media by ID
     * 
     * <p>This endpoint allows you to update specific parameters of an existing media file. You can modify the
     * key-value pairs of the metadata that were provided in the payload during the creation of media from
     * a URL or when uploading the media directly from device.
     * 
     * <p>#### How it works
     * 
     * <p>1. Make a PATCH request to this endpoint. Replace `&lt;mediaId&gt;` with the unique ID (`uploadId`
     * or `id`) of the media you received after uploading to FastPix
     * 
     * <p>2. Include the updated parameters in the request body.
     * 
     * <p>3. The response returns the updated media data, confirming the changes.
     * 
     * <p>4. Monitor the <a
     * href="https://docs.fastpix.io/docs/media-events#videomediaupdated">video.media.updated</a> webhook
     * event to track the update status in your system.
     * 
     * <p>#### Example
     * If a user uploads a video and later needs to change the title, add a new description, or update
     * tags, you can use this endpoint to update the media metadata without re-uploading the entire video.
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @param body 
     * @return {@code CompletableFuture<UpdatedMediaResponse>} - The async response
     */
    public CompletableFuture<UpdatedMediaResponse> updateMedia(@Nonnull String mediaId, @Nonnull UpdatedMediaRequestBody body) {
        return updateMedia(mediaId, body, null);
    }

    /**
     * Update a media by ID
     * 
     * <p>This endpoint allows you to update specific parameters of an existing media file. You can modify the
     * key-value pairs of the metadata that were provided in the payload during the creation of media from
     * a URL or when uploading the media directly from device.
     * 
     * <p>#### How it works
     * 
     * <p>1. Make a PATCH request to this endpoint. Replace `&lt;mediaId&gt;` with the unique ID (`uploadId`
     * or `id`) of the media you received after uploading to FastPix
     * 
     * <p>2. Include the updated parameters in the request body.
     * 
     * <p>3. The response returns the updated media data, confirming the changes.
     * 
     * <p>4. Monitor the <a
     * href="https://docs.fastpix.io/docs/media-events#videomediaupdated">video.media.updated</a> webhook
     * event to track the update status in your system.
     * 
     * <p>#### Example
     * If a user uploads a video and later needs to change the title, add a new description, or update
     * tags, you can use this endpoint to update the media metadata without re-uploading the entire video.
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @param body 
     * @param options additional options
     * @return {@code CompletableFuture<UpdatedMediaResponse>} - The async response
     */
    public CompletableFuture<UpdatedMediaResponse> updateMedia(
            @Nonnull String mediaId, @Nonnull UpdatedMediaRequestBody body,
            @Nullable Options options) {
        UpdatedMediaRequest request = new UpdatedMediaRequest(mediaId, body);
        AsyncRequestOperation<UpdatedMediaRequest, UpdatedMediaResponse> operation
              = new UpdatedMedia.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Delete a media by ID
     * 
     * <p>This endpoint allows you to permanently delete a a specific video or audio media file along with all
     * associated data. If you wish to remove a media from FastPix storage, use this endpoint with the
     * `mediaId` (either `uploadId` or `id`) received during the media's creation or upload.
     * 
     * <p>#### How it works
     * 
     * <p>1. Send a DELETE request to this endpoint. Replace `&lt;mediaId&gt;` with the `uploadId` or the `id`
     * of the media you want to delete.
     * 
     * <p>2. This action is irreversible. Make sure you no longer need the media before proceeding.
     * 
     * <p>Once deleted, the media can’t be retrieved or played back.
     * 
     * <p>3. Monitor the following webhook event: <a
     * href="https://docs.fastpix.io/docs/media-events#videomediadeleted">video.media.deleted</a>
     * 
     * <p>#### Example
     * A user on a video-sharing platform decides to remove an old video from their profile, or suppose
     * you're running a content moderation system, and one of the videos uploaded by a user violates your
     * platform's policies. Using this endpoint, the media is permanently deleted from your library,
     * ensuring it's no longer accessible or viewable by other users.
     * 
     * @return The async call builder
     */
    public DeleteMediaRequestBuilder delete() {
        return new DeleteMediaRequestBuilder(sdkConfiguration);
    }

    /**
     * Delete a media by ID
     * 
     * <p>This endpoint allows you to permanently delete a a specific video or audio media file along with all
     * associated data. If you wish to remove a media from FastPix storage, use this endpoint with the
     * `mediaId` (either `uploadId` or `id`) received during the media's creation or upload.
     * 
     * <p>#### How it works
     * 
     * <p>1. Send a DELETE request to this endpoint. Replace `&lt;mediaId&gt;` with the `uploadId` or the `id`
     * of the media you want to delete.
     * 
     * <p>2. This action is irreversible. Make sure you no longer need the media before proceeding.
     * 
     * <p>Once deleted, the media can’t be retrieved or played back.
     * 
     * <p>3. Monitor the following webhook event: <a
     * href="https://docs.fastpix.io/docs/media-events#videomediadeleted">video.media.deleted</a>
     * 
     * <p>#### Example
     * A user on a video-sharing platform decides to remove an old video from their profile, or suppose
     * you're running a content moderation system, and one of the videos uploaded by a user violates your
     * platform's policies. Using this endpoint, the media is permanently deleted from your library,
     * ensuring it's no longer accessible or viewable by other users.
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @return {@code CompletableFuture<DeleteMediaResponse>} - The async response
     */
    public CompletableFuture<DeleteMediaResponse> delete(@Nonnull String mediaId) {
        return delete(mediaId, null);
    }

    /**
     * Delete a media by ID
     * 
     * <p>This endpoint allows you to permanently delete a a specific video or audio media file along with all
     * associated data. If you wish to remove a media from FastPix storage, use this endpoint with the
     * `mediaId` (either `uploadId` or `id`) received during the media's creation or upload.
     * 
     * <p>#### How it works
     * 
     * <p>1. Send a DELETE request to this endpoint. Replace `&lt;mediaId&gt;` with the `uploadId` or the `id`
     * of the media you want to delete.
     * 
     * <p>2. This action is irreversible. Make sure you no longer need the media before proceeding.
     * 
     * <p>Once deleted, the media can’t be retrieved or played back.
     * 
     * <p>3. Monitor the following webhook event: <a
     * href="https://docs.fastpix.io/docs/media-events#videomediadeleted">video.media.deleted</a>
     * 
     * <p>#### Example
     * A user on a video-sharing platform decides to remove an old video from their profile, or suppose
     * you're running a content moderation system, and one of the videos uploaded by a user violates your
     * platform's policies. Using this endpoint, the media is permanently deleted from your library,
     * ensuring it's no longer accessible or viewable by other users.
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @param options additional options
     * @return {@code CompletableFuture<DeleteMediaResponse>} - The async response
     */
    public CompletableFuture<DeleteMediaResponse> delete(@Nonnull String mediaId, @Nullable Options options) {
        DeleteMediaRequest request = new DeleteMediaRequest(mediaId);
        AsyncRequestOperation<DeleteMediaRequest, DeleteMediaResponse> operation
              = new DeleteMedia.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Cancel ongoing upload
     * 
     * <p>This endpoint allows you to cancel ongoing upload by its `uploadId`. Once cancelled, the upload is
     * marked as cancelled. Use this if a user aborts an upload or if you want to programmatically stop an
     * in-progress upload.
     * 
     * <p>#### How it works
     * 
     * <p>1. Make a PUT request to this endpoint, replacing `{uploadId}` with the unique upload ID received
     * after starting the upload.
     * 2. The response confirms the cancellation and provide the status of the upload.
     * 
     * <p>#### Webhook Events
     * 
     * <p>Once the upload is cancelled, you must receive the webhook event <a
     * href="https://docs.fastpix.io/docs/media-events#videomediauploadcancelled">video.media.upload.cancelled</a>.
     * 
     * <p>#### Example
     * 
     * <p>Suppose a user starts uploading a large video file but decides to cancel before completion. By
     * calling this API, you can immediately stop the upload and free up resources.
     * 
     * @return The async call builder
     */
    public CancelUploadRequestBuilder cancelUpload() {
        return new CancelUploadRequestBuilder(sdkConfiguration);
    }

    /**
     * Cancel ongoing upload
     * 
     * <p>This endpoint allows you to cancel ongoing upload by its `uploadId`. Once cancelled, the upload is
     * marked as cancelled. Use this if a user aborts an upload or if you want to programmatically stop an
     * in-progress upload.
     * 
     * <p>#### How it works
     * 
     * <p>1. Make a PUT request to this endpoint, replacing `{uploadId}` with the unique upload ID received
     * after starting the upload.
     * 2. The response confirms the cancellation and provide the status of the upload.
     * 
     * <p>#### Webhook Events
     * 
     * <p>Once the upload is cancelled, you must receive the webhook event <a
     * href="https://docs.fastpix.io/docs/media-events#videomediauploadcancelled">video.media.upload.cancelled</a>.
     * 
     * <p>#### Example
     * 
     * <p>Suppose a user starts uploading a large video file but decides to cancel before completion. By
     * calling this API, you can immediately stop the upload and free up resources.
     * 
     * @param uploadId When uploading the media, FastPix assigns a universally unique identifier with a maximum length of 255 characters.
     * @return {@code CompletableFuture<CancelUploadResponse>} - The async response
     */
    public CompletableFuture<CancelUploadResponse> cancelUpload(@Nonnull String uploadId) {
        return cancelUpload(uploadId, null);
    }

    /**
     * Cancel ongoing upload
     * 
     * <p>This endpoint allows you to cancel ongoing upload by its `uploadId`. Once cancelled, the upload is
     * marked as cancelled. Use this if a user aborts an upload or if you want to programmatically stop an
     * in-progress upload.
     * 
     * <p>#### How it works
     * 
     * <p>1. Make a PUT request to this endpoint, replacing `{uploadId}` with the unique upload ID received
     * after starting the upload.
     * 2. The response confirms the cancellation and provide the status of the upload.
     * 
     * <p>#### Webhook Events
     * 
     * <p>Once the upload is cancelled, you must receive the webhook event <a
     * href="https://docs.fastpix.io/docs/media-events#videomediauploadcancelled">video.media.upload.cancelled</a>.
     * 
     * <p>#### Example
     * 
     * <p>Suppose a user starts uploading a large video file but decides to cancel before completion. By
     * calling this API, you can immediately stop the upload and free up resources.
     * 
     * @param uploadId When uploading the media, FastPix assigns a universally unique identifier with a maximum length of 255 characters.
     * @param options additional options
     * @return {@code CompletableFuture<CancelUploadResponse>} - The async response
     */
    public CompletableFuture<CancelUploadResponse> cancelUpload(@Nonnull String uploadId, @Nullable Options options) {
        CancelUploadRequest request = new CancelUploadRequest(uploadId);
        AsyncRequestOperation<CancelUploadRequest, CancelUploadResponse> operation
              = new CancelUpload.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Update audio / subtitle track
     * 
     * <p>This endpoint allows you to update an existing audio or subtitle track associated with a media file.
     * When updating a track, you must provide the new track `url`, `languageName`, and `languageCode`,
     * ensuring all three parameters are included in the request.
     * 
     * <p>#### How it works
     * 
     * <p>1. Send a PATCH request to this endpoint, replacing `{mediaId}` with the media ID, and `{trackId}`
     * with the ID of the track you want to update.
     * 
     * <p>2. Provide the necessary details in the request body.
     * 
     * <p>3. Receive a response confirming the track update.
     * 
     * <p>#### Webhook Events
     * 
     * <p>After updating a track, your system must receive webhook notifications:
     * 
     * <p>1. After successfully updating a track, your system must receive the webhook event <a
     * href="https://docs.fastpix.io/docs/transform-media-events#videomediatrackupdated">video.media.track.updated</a>.
     * 
     * <p>2. Once the new track is processed and ready, you must receive the webhook event <a
     * href="https://docs.fastpix.io/docs/transform-media-events#videomediatrackready">video.media.track.ready</a>.
     * 
     * <p>3. Once the media file is updated with the new track details, a <a
     * href="https://docs.fastpix.io/docs/media-events#videomediaupdated">video.media.updated</a> event
     * must be triggered.
     * 
     * <p>#### Example
     * Suppose you previously added a French subtitle track to a video but now need to update it with a
     * different file. By calling this API, you can replace the existing subtitle file (.vtt) with a new
     * one while keeping the same track ID. This is useful when:
     * 
     * <p>- The original track file has errors and needs correction.
     * - You want to improve subtitle translations or replace an audio track with a better-quality version.
     * 
     * <p>Related guides: <a href="https://docs.fastpix.io/docs/manage-subtitle-tracks">Add own subtitle
     * tracks</a>, <a href="https://docs.fastpix.io/docs/manage-audio-tracks">Add own audio tracks</a>
     * 
     * @return The async call builder
     */
    public UpdateMediaTrackRequestBuilder updateTrack() {
        return new UpdateMediaTrackRequestBuilder(sdkConfiguration);
    }

    /**
     * Update audio / subtitle track
     * 
     * <p>This endpoint allows you to update an existing audio or subtitle track associated with a media file.
     * When updating a track, you must provide the new track `url`, `languageName`, and `languageCode`,
     * ensuring all three parameters are included in the request.
     * 
     * <p>#### How it works
     * 
     * <p>1. Send a PATCH request to this endpoint, replacing `{mediaId}` with the media ID, and `{trackId}`
     * with the ID of the track you want to update.
     * 
     * <p>2. Provide the necessary details in the request body.
     * 
     * <p>3. Receive a response confirming the track update.
     * 
     * <p>#### Webhook Events
     * 
     * <p>After updating a track, your system must receive webhook notifications:
     * 
     * <p>1. After successfully updating a track, your system must receive the webhook event <a
     * href="https://docs.fastpix.io/docs/transform-media-events#videomediatrackupdated">video.media.track.updated</a>.
     * 
     * <p>2. Once the new track is processed and ready, you must receive the webhook event <a
     * href="https://docs.fastpix.io/docs/transform-media-events#videomediatrackready">video.media.track.ready</a>.
     * 
     * <p>3. Once the media file is updated with the new track details, a <a
     * href="https://docs.fastpix.io/docs/media-events#videomediaupdated">video.media.updated</a> event
     * must be triggered.
     * 
     * <p>#### Example
     * Suppose you previously added a French subtitle track to a video but now need to update it with a
     * different file. By calling this API, you can replace the existing subtitle file (.vtt) with a new
     * one while keeping the same track ID. This is useful when:
     * 
     * <p>- The original track file has errors and needs correction.
     * - You want to improve subtitle translations or replace an audio track with a better-quality version.
     * 
     * <p>Related guides: <a href="https://docs.fastpix.io/docs/manage-subtitle-tracks">Add own subtitle
     * tracks</a>, <a href="https://docs.fastpix.io/docs/manage-audio-tracks">Add own audio tracks</a>
     * 
     * @param trackId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @param body Contains details about the track being added to the media file.
     * @return {@code CompletableFuture<UpdateMediaTrackResponse>} - The async response
     */
    public CompletableFuture<UpdateMediaTrackResponse> updateTrack(
            @Nonnull String trackId, @Nonnull String mediaId,
            @Nonnull UpdateTrackRequest body) {
        return updateTrack(
                trackId, mediaId, body,
                null);
    }

    /**
     * Update audio / subtitle track
     * 
     * <p>This endpoint allows you to update an existing audio or subtitle track associated with a media file.
     * When updating a track, you must provide the new track `url`, `languageName`, and `languageCode`,
     * ensuring all three parameters are included in the request.
     * 
     * <p>#### How it works
     * 
     * <p>1. Send a PATCH request to this endpoint, replacing `{mediaId}` with the media ID, and `{trackId}`
     * with the ID of the track you want to update.
     * 
     * <p>2. Provide the necessary details in the request body.
     * 
     * <p>3. Receive a response confirming the track update.
     * 
     * <p>#### Webhook Events
     * 
     * <p>After updating a track, your system must receive webhook notifications:
     * 
     * <p>1. After successfully updating a track, your system must receive the webhook event <a
     * href="https://docs.fastpix.io/docs/transform-media-events#videomediatrackupdated">video.media.track.updated</a>.
     * 
     * <p>2. Once the new track is processed and ready, you must receive the webhook event <a
     * href="https://docs.fastpix.io/docs/transform-media-events#videomediatrackready">video.media.track.ready</a>.
     * 
     * <p>3. Once the media file is updated with the new track details, a <a
     * href="https://docs.fastpix.io/docs/media-events#videomediaupdated">video.media.updated</a> event
     * must be triggered.
     * 
     * <p>#### Example
     * Suppose you previously added a French subtitle track to a video but now need to update it with a
     * different file. By calling this API, you can replace the existing subtitle file (.vtt) with a new
     * one while keeping the same track ID. This is useful when:
     * 
     * <p>- The original track file has errors and needs correction.
     * - You want to improve subtitle translations or replace an audio track with a better-quality version.
     * 
     * <p>Related guides: <a href="https://docs.fastpix.io/docs/manage-subtitle-tracks">Add own subtitle
     * tracks</a>, <a href="https://docs.fastpix.io/docs/manage-audio-tracks">Add own audio tracks</a>
     * 
     * @param trackId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @param body Contains details about the track being added to the media file.
     * @param options additional options
     * @return {@code CompletableFuture<UpdateMediaTrackResponse>} - The async response
     */
    public CompletableFuture<UpdateMediaTrackResponse> updateTrack(
            @Nonnull String trackId, @Nonnull String mediaId,
            @Nonnull UpdateTrackRequest body, @Nullable Options options) {
        UpdateMediaTrackRequest request = new UpdateMediaTrackRequest(trackId, mediaId, body);
        AsyncRequestOperation<UpdateMediaTrackRequest, UpdateMediaTrackResponse> operation
              = new UpdateMediaTrack.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Generate video chapters
     * 
     * <p>This endpoint enables you to generate chapters for an existing media file.
     * 
     * <p>#### How it works
     * 1. Make a `PATCH` request to this endpoint, replacing `&lt;mediaId&gt;` with the ID of the media for
     * which you want to generate chapters.
     * 2. Include the `chapters` parameter in the request body to enable.
     * 3. The response contains the updated media data, confirming the changes made.
     * 
     * <p>You can use the <a
     * href="https://docs.fastpix.io/docs/ai-events#videomediaaichaptersready">video.mediaAI.chapters.ready</a>
     * webhook event to track and notify about the chapters generation.
     * 
     * <p>**Use case:** This is particularly useful when a user uploads a video and later decides to enable
     * chapters without re-uploading the entire video.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/generate-video-chapters">Video chapters</a>
     * 
     * @return The async call builder
     */
    public UpdateMediaChaptersRequestBuilder updateChapters() {
        return new UpdateMediaChaptersRequestBuilder(sdkConfiguration);
    }

    /**
     * Generate video chapters
     * 
     * <p>This endpoint enables you to generate chapters for an existing media file.
     * 
     * <p>#### How it works
     * 1. Make a `PATCH` request to this endpoint, replacing `&lt;mediaId&gt;` with the ID of the media for
     * which you want to generate chapters.
     * 2. Include the `chapters` parameter in the request body to enable.
     * 3. The response contains the updated media data, confirming the changes made.
     * 
     * <p>You can use the <a
     * href="https://docs.fastpix.io/docs/ai-events#videomediaaichaptersready">video.mediaAI.chapters.ready</a>
     * webhook event to track and notify about the chapters generation.
     * 
     * <p>**Use case:** This is particularly useful when a user uploads a video and later decides to enable
     * chapters without re-uploading the entire video.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/generate-video-chapters">Video chapters</a>
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     *         
     * @param body 
     * @return {@code CompletableFuture<UpdateMediaChaptersResponse>} - The async response
     */
    public CompletableFuture<UpdateMediaChaptersResponse> updateChapters(@Nonnull String mediaId, @Nonnull UpdateMediaChaptersRequestBody body) {
        return updateChapters(mediaId, body, null);
    }

    /**
     * Generate video chapters
     * 
     * <p>This endpoint enables you to generate chapters for an existing media file.
     * 
     * <p>#### How it works
     * 1. Make a `PATCH` request to this endpoint, replacing `&lt;mediaId&gt;` with the ID of the media for
     * which you want to generate chapters.
     * 2. Include the `chapters` parameter in the request body to enable.
     * 3. The response contains the updated media data, confirming the changes made.
     * 
     * <p>You can use the <a
     * href="https://docs.fastpix.io/docs/ai-events#videomediaaichaptersready">video.mediaAI.chapters.ready</a>
     * webhook event to track and notify about the chapters generation.
     * 
     * <p>**Use case:** This is particularly useful when a user uploads a video and later decides to enable
     * chapters without re-uploading the entire video.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/generate-video-chapters">Video chapters</a>
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     *         
     * @param body 
     * @param options additional options
     * @return {@code CompletableFuture<UpdateMediaChaptersResponse>} - The async response
     */
    public CompletableFuture<UpdateMediaChaptersResponse> updateChapters(
            @Nonnull String mediaId, @Nonnull UpdateMediaChaptersRequestBody body,
            @Nullable Options options) {
        UpdateMediaChaptersRequest request = new UpdateMediaChaptersRequest(mediaId, body);
        AsyncRequestOperation<UpdateMediaChaptersRequest, UpdateMediaChaptersResponse> operation
              = new UpdateMediaChapters.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Update the mp4Support of a media by ID
     * 
     * <p>This endpoint allows you to update the `mp4Support` setting of an existing media file using its
     * media ID. You can specify the MP4 support level, such as `none`, `capped_4k`, `audioOnly`, or a
     * combination of `audioOnly`, `capped_4k`, in the request payload.
     * 
     * <p>#### How it works
     * 
     * <p>1. Send a PATCH request to this endpoint, replacing `{mediaId}` with the media ID.
     * 
     * <p>2. Provide the desired `mp4Support` value in the request body.
     * 
     * <p>3. You receive a response confirming the update, including the media’s updated MP4 support status.
     * 
     * <p>#### MP4 Support Options
     * 
     * <p>- `none` – MP4 support is disabled for this media.
     * 
     * <p>- `capped_4k` – Generates MP4 renditions up to 4K resolution.
     * 
     * <p>- `audioOnly` – Generates an M4A file that contains only the audio track.
     * 
     * <p>- `audioOnly,capped_4k` – Generates both an audio-only M4A file and MP4 renditions up to 4K
     * resolution.
     * 
     * <p>#### Webhook events
     * 
     * <p>- <a
     * href="https://docs.fastpix.io/docs/transform-media-events#videomediamp4supportready">video.media.mp4Support.ready</a>
     * – Triggered when the MP4 support setting is successfully updated.
     * 
     * <p>#### Example
     * Suppose you have a video uploaded to the FastPix platform, and you want to allow users to download
     * the video in MP4 format. By setting "mp4Support": "capped_4k", the system generates an MP4 rendition
     * of the video up to 4K resolution, making it available for download through the stream
     * URL(`https://stream.fastpix.io/{playbackId}/{capped-4k.mp4 | audio.m4a}`). If you want users to
     * stream only the audio from the media file, you can set "mp4Support": "audioOnly".
     * 
     * <p>This provides an audio-only stream URL that allows users to listen to the media without video. By
     * setting "mp4Support": "audioOnly,capped_4k", both options are enabled. Users can download the MP4
     * video and also stream just the audio version of the media.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/mp4-support-for-offline-viewing">Use MP4
     * support for offline viewing</a>
     * 
     * @return The async call builder
     */
    public UpdatedMp4SupportRequestBuilder updateMp4Support() {
        return new UpdatedMp4SupportRequestBuilder(sdkConfiguration);
    }

    /**
     * Update the mp4Support of a media by ID
     * 
     * <p>This endpoint allows you to update the `mp4Support` setting of an existing media file using its
     * media ID. You can specify the MP4 support level, such as `none`, `capped_4k`, `audioOnly`, or a
     * combination of `audioOnly`, `capped_4k`, in the request payload.
     * 
     * <p>#### How it works
     * 
     * <p>1. Send a PATCH request to this endpoint, replacing `{mediaId}` with the media ID.
     * 
     * <p>2. Provide the desired `mp4Support` value in the request body.
     * 
     * <p>3. You receive a response confirming the update, including the media’s updated MP4 support status.
     * 
     * <p>#### MP4 Support Options
     * 
     * <p>- `none` – MP4 support is disabled for this media.
     * 
     * <p>- `capped_4k` – Generates MP4 renditions up to 4K resolution.
     * 
     * <p>- `audioOnly` – Generates an M4A file that contains only the audio track.
     * 
     * <p>- `audioOnly,capped_4k` – Generates both an audio-only M4A file and MP4 renditions up to 4K
     * resolution.
     * 
     * <p>#### Webhook events
     * 
     * <p>- <a
     * href="https://docs.fastpix.io/docs/transform-media-events#videomediamp4supportready">video.media.mp4Support.ready</a>
     * – Triggered when the MP4 support setting is successfully updated.
     * 
     * <p>#### Example
     * Suppose you have a video uploaded to the FastPix platform, and you want to allow users to download
     * the video in MP4 format. By setting "mp4Support": "capped_4k", the system generates an MP4 rendition
     * of the video up to 4K resolution, making it available for download through the stream
     * URL(`https://stream.fastpix.io/{playbackId}/{capped-4k.mp4 | audio.m4a}`). If you want users to
     * stream only the audio from the media file, you can set "mp4Support": "audioOnly".
     * 
     * <p>This provides an audio-only stream URL that allows users to listen to the media without video. By
     * setting "mp4Support": "audioOnly,capped_4k", both options are enabled. Users can download the MP4
     * video and also stream just the audio version of the media.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/mp4-support-for-offline-viewing">Use MP4
     * support for offline viewing</a>
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     *         
     * @param body 
     * @return {@code CompletableFuture<UpdatedMp4SupportResponse>} - The async response
     */
    public CompletableFuture<UpdatedMp4SupportResponse> updateMp4Support(@Nonnull String mediaId, @Nonnull UpdatedMp4SupportRequestBody body) {
        return updateMp4Support(mediaId, body, null);
    }

    /**
     * Update the mp4Support of a media by ID
     * 
     * <p>This endpoint allows you to update the `mp4Support` setting of an existing media file using its
     * media ID. You can specify the MP4 support level, such as `none`, `capped_4k`, `audioOnly`, or a
     * combination of `audioOnly`, `capped_4k`, in the request payload.
     * 
     * <p>#### How it works
     * 
     * <p>1. Send a PATCH request to this endpoint, replacing `{mediaId}` with the media ID.
     * 
     * <p>2. Provide the desired `mp4Support` value in the request body.
     * 
     * <p>3. You receive a response confirming the update, including the media’s updated MP4 support status.
     * 
     * <p>#### MP4 Support Options
     * 
     * <p>- `none` – MP4 support is disabled for this media.
     * 
     * <p>- `capped_4k` – Generates MP4 renditions up to 4K resolution.
     * 
     * <p>- `audioOnly` – Generates an M4A file that contains only the audio track.
     * 
     * <p>- `audioOnly,capped_4k` – Generates both an audio-only M4A file and MP4 renditions up to 4K
     * resolution.
     * 
     * <p>#### Webhook events
     * 
     * <p>- <a
     * href="https://docs.fastpix.io/docs/transform-media-events#videomediamp4supportready">video.media.mp4Support.ready</a>
     * – Triggered when the MP4 support setting is successfully updated.
     * 
     * <p>#### Example
     * Suppose you have a video uploaded to the FastPix platform, and you want to allow users to download
     * the video in MP4 format. By setting "mp4Support": "capped_4k", the system generates an MP4 rendition
     * of the video up to 4K resolution, making it available for download through the stream
     * URL(`https://stream.fastpix.io/{playbackId}/{capped-4k.mp4 | audio.m4a}`). If you want users to
     * stream only the audio from the media file, you can set "mp4Support": "audioOnly".
     * 
     * <p>This provides an audio-only stream URL that allows users to listen to the media without video. By
     * setting "mp4Support": "audioOnly,capped_4k", both options are enabled. Users can download the MP4
     * video and also stream just the audio version of the media.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/mp4-support-for-offline-viewing">Use MP4
     * support for offline viewing</a>
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     *         
     * @param body 
     * @param options additional options
     * @return {@code CompletableFuture<UpdatedMp4SupportResponse>} - The async response
     */
    public CompletableFuture<UpdatedMp4SupportResponse> updateMp4Support(
            @Nonnull String mediaId, @Nonnull UpdatedMp4SupportRequestBody body,
            @Nullable Options options) {
        UpdatedMp4SupportRequest request = new UpdatedMp4SupportRequest(mediaId, body);
        AsyncRequestOperation<UpdatedMp4SupportRequest, UpdatedMp4SupportResponse> operation
              = new UpdatedMp4Support.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Get all unused upload URLs
     * 
     * <p>This endpoint retrieves a paginated list of all unused upload signed URLs within your organization.
     * It provides comprehensive metadata including upload IDs, creation dates, status, and URLs, helping
     * you manage your media resources efficiently.
     * 
     * <p>An unused upload URL is a signed URL that gets generated when an user initiates upload but never
     * completed the upload process. This can happen due to reasons like network issues, manual
     * cancellation of upload, browser/app crashes or session timeouts.These URLs remain in the system as
     * "unused" since they were created but never resulted in a successful media file upload.
     * 
     * <p>#### How it works
     * 
     * <p>- The endpoint returns metadata for all unused upload URLs in your organization's library.
     * - Results are paginated to manage large datasets effectively.
     * - Signed URLs expire after 24 hours from creation.
     * - Each entry includes full metadata about the unused upload.
     * 
     * <p>#### Example
     * 
     * <p>A video management team at a media organization regularly uploads content but often forgets to
     * delete or use unused uploads. These unused uploads have signed URLs that expire after 24 hours and
     * need to be managed efficiently. By using this API, the team can retrieve metadata for all unused
     * uploads, identify expired signed URLs, and decide whether to regenerate URLs, reuse the uploads, or
     * delete them.
     * 
     * @return The async call builder
     */
    public ListUploadsRequestBuilder listUploads() {
        return new ListUploadsRequestBuilder(sdkConfiguration);
    }

    /**
     * Get all unused upload URLs
     * 
     * <p>This endpoint retrieves a paginated list of all unused upload signed URLs within your organization.
     * It provides comprehensive metadata including upload IDs, creation dates, status, and URLs, helping
     * you manage your media resources efficiently.
     * 
     * <p>An unused upload URL is a signed URL that gets generated when an user initiates upload but never
     * completed the upload process. This can happen due to reasons like network issues, manual
     * cancellation of upload, browser/app crashes or session timeouts.These URLs remain in the system as
     * "unused" since they were created but never resulted in a successful media file upload.
     * 
     * <p>#### How it works
     * 
     * <p>- The endpoint returns metadata for all unused upload URLs in your organization's library.
     * - Results are paginated to manage large datasets effectively.
     * - Signed URLs expire after 24 hours from creation.
     * - Each entry includes full metadata about the unused upload.
     * 
     * <p>#### Example
     * 
     * <p>A video management team at a media organization regularly uploads content but often forgets to
     * delete or use unused uploads. These unused uploads have signed URLs that expire after 24 hours and
     * need to be managed efficiently. By using this API, the team can retrieve metadata for all unused
     * uploads, identify expired signed URLs, and decide whether to regenerate URLs, reuse the uploads, or
     * delete them.
     * 
     * @return {@code CompletableFuture<ListUploadsResponse>} - The async response
     */
    public CompletableFuture<ListUploadsResponse> listUploadsDirect() {
        return listUploads(
                null, null, null,
                null);
    }

    /**
     * Get all unused upload URLs
     * 
     * <p>This endpoint retrieves a paginated list of all unused upload signed URLs within your organization.
     * It provides comprehensive metadata including upload IDs, creation dates, status, and URLs, helping
     * you manage your media resources efficiently.
     * 
     * <p>An unused upload URL is a signed URL that gets generated when an user initiates upload but never
     * completed the upload process. This can happen due to reasons like network issues, manual
     * cancellation of upload, browser/app crashes or session timeouts.These URLs remain in the system as
     * "unused" since they were created but never resulted in a successful media file upload.
     * 
     * <p>#### How it works
     * 
     * <p>- The endpoint returns metadata for all unused upload URLs in your organization's library.
     * - Results are paginated to manage large datasets effectively.
     * - Signed URLs expire after 24 hours from creation.
     * - Each entry includes full metadata about the unused upload.
     * 
     * <p>#### Example
     * 
     * <p>A video management team at a media organization regularly uploads content but often forgets to
     * delete or use unused uploads. These unused uploads have signed URLs that expire after 24 hours and
     * need to be managed efficiently. By using this API, the team can retrieve metadata for all unused
     * uploads, identify expired signed URLs, and decide whether to regenerate URLs, reuse the uploads, or
     * delete them.
     * 
     * @param limit Limit specifies the maximum number of items to display per page.
     * @param offset Offset determines the starting point for data retrieval within a paginated list.
     * @param orderBy The values in the list can be arranged in two ways: DESC (Descending) or ASC (Ascending).
     * @param options additional options
     * @return {@code CompletableFuture<ListUploadsResponse>} - The async response
     */
    public CompletableFuture<ListUploadsResponse> listUploads(
            @Nullable Long limit, @Nullable Long offset,
            @Nullable SortOrder orderBy, @Nullable Options options) {
        ListUploadsRequest request = new ListUploadsRequest(limit, offset, orderBy);
        AsyncRequestOperation<ListUploadsRequest, ListUploadsResponse> operation
              = new ListUploads.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Get all clips of a media
     * 
     * <p>This endpoint retrieves a list of all media clips associated with a given source media ID. It helps
     * you organize and manage media efficiently by providing metadata such as clip media IDs and other
     * relevant details.
     * 
     * <p>A media clip is a segmented portion of an original media file (source media). Clips are often
     * created for various purposes such as previews, highlights, or customized edits. This API allows you
     * to fetch all such clips linked to a specific source media, making it easier to track and manage
     * clips.
     * 
     * <p>#### How it works
     * 
     * <p>- The endpoint returns metadata for all media clips associated with the given `mediaId`.
     * - Results are paginated to efficiently handle large datasets.
     * - Each entry includes detailed metadata such as media `id`, `duration`, and `status`.
     * - Helps in organizing clips effectively by providing structured information.
     * 
     * <p>#### Example
     * 
     * <p>Imagine you’re managing a video editing platform where users upload full-length videos and create
     * short clips for social media sharing. To keep track of all clips linked to a particular video, you
     * call this API with the sourceMediaId. The response provides a list of all associated clips, allowing
     * you to manage, edit, or repurpose them as needed.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/create-clips-from-existing-media">Create clips
     * from existing media</a>
     * 
     * @return The async call builder
     */
    public GetMediaClipsRequestBuilder getMediaClips() {
        return new GetMediaClipsRequestBuilder(sdkConfiguration);
    }

    /**
     * Get all clips of a media
     * 
     * <p>This endpoint retrieves a list of all media clips associated with a given source media ID. It helps
     * you organize and manage media efficiently by providing metadata such as clip media IDs and other
     * relevant details.
     * 
     * <p>A media clip is a segmented portion of an original media file (source media). Clips are often
     * created for various purposes such as previews, highlights, or customized edits. This API allows you
     * to fetch all such clips linked to a specific source media, making it easier to track and manage
     * clips.
     * 
     * <p>#### How it works
     * 
     * <p>- The endpoint returns metadata for all media clips associated with the given `mediaId`.
     * - Results are paginated to efficiently handle large datasets.
     * - Each entry includes detailed metadata such as media `id`, `duration`, and `status`.
     * - Helps in organizing clips effectively by providing structured information.
     * 
     * <p>#### Example
     * 
     * <p>Imagine you’re managing a video editing platform where users upload full-length videos and create
     * short clips for social media sharing. To keep track of all clips linked to a particular video, you
     * call this API with the sourceMediaId. The response provides a list of all associated clips, allowing
     * you to manage, edit, or repurpose them as needed.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/create-clips-from-existing-media">Create clips
     * from existing media</a>
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @return {@code CompletableFuture<GetMediaClipsResponse>} - The async response
     */
    public CompletableFuture<GetMediaClipsResponse> getMediaClips(@Nonnull String mediaId) {
        return getMediaClips(
                mediaId, null, null,
                null, null);
    }

    /**
     * Get all clips of a media
     * 
     * <p>This endpoint retrieves a list of all media clips associated with a given source media ID. It helps
     * you organize and manage media efficiently by providing metadata such as clip media IDs and other
     * relevant details.
     * 
     * <p>A media clip is a segmented portion of an original media file (source media). Clips are often
     * created for various purposes such as previews, highlights, or customized edits. This API allows you
     * to fetch all such clips linked to a specific source media, making it easier to track and manage
     * clips.
     * 
     * <p>#### How it works
     * 
     * <p>- The endpoint returns metadata for all media clips associated with the given `mediaId`.
     * - Results are paginated to efficiently handle large datasets.
     * - Each entry includes detailed metadata such as media `id`, `duration`, and `status`.
     * - Helps in organizing clips effectively by providing structured information.
     * 
     * <p>#### Example
     * 
     * <p>Imagine you’re managing a video editing platform where users upload full-length videos and create
     * short clips for social media sharing. To keep track of all clips linked to a particular video, you
     * call this API with the sourceMediaId. The response provides a list of all associated clips, allowing
     * you to manage, edit, or repurpose them as needed.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/create-clips-from-existing-media">Create clips
     * from existing media</a>
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @param offset Offset determines the starting point for data retrieval within a paginated list.
     * @param limit The number of media clips to retrieve per request.
     * @param orderBy The values in the list can be arranged in two ways: DESC (Descending) or ASC (Ascending).
     * @param options additional options
     * @return {@code CompletableFuture<GetMediaClipsResponse>} - The async response
     */
    public CompletableFuture<GetMediaClipsResponse> getMediaClips(
            @Nonnull String mediaId, @Nullable Long offset,
            @Nullable Long limit, @Nullable SortOrder orderBy,
            @Nullable Options options) {
        GetMediaClipsRequest request = new GetMediaClipsRequest(
                mediaId, offset, limit,
                orderBy);
        AsyncRequestOperation<GetMediaClipsRequest, GetMediaClipsResponse> operation
              = new GetMediaClips.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }

}
