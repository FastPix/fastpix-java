/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
package org.openapis.openapi;

import static org.openapis.openapi.operations.Operations.RequestOperation;

import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;
import org.openapis.openapi.models.operations.DeleteSimulcastOfStreamRequest;
import org.openapis.openapi.models.operations.DeleteSimulcastOfStreamRequestBuilder;
import org.openapis.openapi.models.operations.DeleteSimulcastOfStreamResponse;
import org.openapis.openapi.models.operations.GetSpecificSimulcastOfStreamRequest;
import org.openapis.openapi.models.operations.GetSpecificSimulcastOfStreamRequestBuilder;
import org.openapis.openapi.models.operations.GetSpecificSimulcastOfStreamResponse;
import org.openapis.openapi.operations.DeleteSimulcastOfStream;
import org.openapis.openapi.operations.GetSpecificSimulcastOfStream;
import org.openapis.openapi.utils.Headers;
import org.openapis.openapi.utils.Options;


public class SimulcastStreams {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncSimulcastStreams asyncSDK;

    SimulcastStreams(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncSimulcastStreams(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncSimulcastStreams async() {
        return asyncSDK;
    }

    /**
     * Delete a simulcast
     * 
     * <p>Deletes a simulcast using its unique simulcastId, which you received during the simulcast creation
     * process. Deleting a simulcast stops the broadcast to the associated platform, while the parent
     * stream continues if it’s live. This action can’t be undone, and you must create a new simulcast to
     * resume streaming to the same platform.
     * 
     * <p>Webhook event: <a
     * href="https://docs.fastpix.io/docs/live-events#videolive_streamsimulcast_targetdeleted">video.live_stream.simulcast_target.deleted</a>
     * 
     * <p>#### Example
     * A broadcaster may need to stop simulcasting to one platform while keeping the stream active on
     * others. For example, a tech company is simulcasting a product launch across multiple platforms.
     * Midway through the event, they decide to stop the simulcast on Facebook due to performance issues
     * but continue streaming on YouTube.
     * 
     * <p>They use this API to delete the Facebook simulcast target.
     * 
     * @return The call builder
     */
    public DeleteSimulcastOfStreamRequestBuilder delete() {
        return new DeleteSimulcastOfStreamRequestBuilder(sdkConfiguration);
    }

    /**
     * Delete a simulcast
     * 
     * <p>Deletes a simulcast using its unique simulcastId, which you received during the simulcast creation
     * process. Deleting a simulcast stops the broadcast to the associated platform, while the parent
     * stream continues if it’s live. This action can’t be undone, and you must create a new simulcast to
     * resume streaming to the same platform.
     * 
     * <p>Webhook event: <a
     * href="https://docs.fastpix.io/docs/live-events#videolive_streamsimulcast_targetdeleted">video.live_stream.simulcast_target.deleted</a>
     * 
     * <p>#### Example
     * A broadcaster may need to stop simulcasting to one platform while keeping the stream active on
     * others. For example, a tech company is simulcasting a product launch across multiple platforms.
     * Midway through the event, they decide to stop the simulcast on Facebook due to performance issues
     * but continue streaming on YouTube.
     * 
     * <p>They use this API to delete the Facebook simulcast target.
     * 
     * @param streamId After creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @param simulcastId When you create the new simulcast, FastPix assign a universal unique identifier which can contain a maximum of 255 characters.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DeleteSimulcastOfStreamResponse delete(@Nonnull String streamId, @Nonnull String simulcastId) {
        return delete(streamId, simulcastId, null);
    }

    /**
     * Delete a simulcast
     * 
     * <p>Deletes a simulcast using its unique simulcastId, which you received during the simulcast creation
     * process. Deleting a simulcast stops the broadcast to the associated platform, while the parent
     * stream continues if it’s live. This action can’t be undone, and you must create a new simulcast to
     * resume streaming to the same platform.
     * 
     * <p>Webhook event: <a
     * href="https://docs.fastpix.io/docs/live-events#videolive_streamsimulcast_targetdeleted">video.live_stream.simulcast_target.deleted</a>
     * 
     * <p>#### Example
     * A broadcaster may need to stop simulcasting to one platform while keeping the stream active on
     * others. For example, a tech company is simulcasting a product launch across multiple platforms.
     * Midway through the event, they decide to stop the simulcast on Facebook due to performance issues
     * but continue streaming on YouTube.
     * 
     * <p>They use this API to delete the Facebook simulcast target.
     * 
     * @param streamId After creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @param simulcastId When you create the new simulcast, FastPix assign a universal unique identifier which can contain a maximum of 255 characters.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DeleteSimulcastOfStreamResponse delete(
            @Nonnull String streamId, @Nonnull String simulcastId,
            @Nullable Options options) {
        DeleteSimulcastOfStreamRequest request = new DeleteSimulcastOfStreamRequest(streamId, simulcastId);
        RequestOperation<DeleteSimulcastOfStreamRequest, DeleteSimulcastOfStreamResponse> operation
              = new DeleteSimulcastOfStream.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Get a specific simulcast
     * 
     * <p>Retrieves the details of a specific simulcast associated with a parent live stream. By providing
     * both the `streamId` of the parent stream and the `simulcastId`, FastPix returns detailed information
     * about the simulcast, such as the stream URL, the status of the simulcast, and metadata.
     * 
     * <p>#### Example
     * This endpoint can be used to verify the status of the simulcast on external platforms before the
     * live stream begins. For example, before starting a live gaming event, the organizer wants to ensure
     * that the simulcast to Twitch is set up correctly. They retrieve the simulcast information to confirm
     * that everything is properly configured.
     * 
     * @return The call builder
     */
    public GetSpecificSimulcastOfStreamRequestBuilder getSpecific() {
        return new GetSpecificSimulcastOfStreamRequestBuilder(sdkConfiguration);
    }

    /**
     * Get a specific simulcast
     * 
     * <p>Retrieves the details of a specific simulcast associated with a parent live stream. By providing
     * both the `streamId` of the parent stream and the `simulcastId`, FastPix returns detailed information
     * about the simulcast, such as the stream URL, the status of the simulcast, and metadata.
     * 
     * <p>#### Example
     * This endpoint can be used to verify the status of the simulcast on external platforms before the
     * live stream begins. For example, before starting a live gaming event, the organizer wants to ensure
     * that the simulcast to Twitch is set up correctly. They retrieve the simulcast information to confirm
     * that everything is properly configured.
     * 
     * @param streamId After creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @param simulcastId When you create the new simulcast, FastPix assign a universal unique identifier which can contain a maximum of 255 characters.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetSpecificSimulcastOfStreamResponse getSpecific(@Nonnull String streamId, @Nonnull String simulcastId) {
        return getSpecific(streamId, simulcastId, null);
    }

    /**
     * Get a specific simulcast
     * 
     * <p>Retrieves the details of a specific simulcast associated with a parent live stream. By providing
     * both the `streamId` of the parent stream and the `simulcastId`, FastPix returns detailed information
     * about the simulcast, such as the stream URL, the status of the simulcast, and metadata.
     * 
     * <p>#### Example
     * This endpoint can be used to verify the status of the simulcast on external platforms before the
     * live stream begins. For example, before starting a live gaming event, the organizer wants to ensure
     * that the simulcast to Twitch is set up correctly. They retrieve the simulcast information to confirm
     * that everything is properly configured.
     * 
     * @param streamId After creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @param simulcastId When you create the new simulcast, FastPix assign a universal unique identifier which can contain a maximum of 255 characters.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetSpecificSimulcastOfStreamResponse getSpecific(
            @Nonnull String streamId, @Nonnull String simulcastId,
            @Nullable Options options) {
        GetSpecificSimulcastOfStreamRequest request = new GetSpecificSimulcastOfStreamRequest(streamId, simulcastId);
        RequestOperation<GetSpecificSimulcastOfStreamRequest, GetSpecificSimulcastOfStreamResponse> operation
              = new GetSpecificSimulcastOfStream.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
