/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
package org.openapis.openapi;

import static org.openapis.openapi.operations.Operations.RequestOperation;

import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;
import org.openapis.openapi.models.components.PatchLiveStreamRequest;
import org.openapis.openapi.models.operations.GetLiveStreamByIdRequest;
import org.openapis.openapi.models.operations.GetLiveStreamByIdRequestBuilder;
import org.openapis.openapi.models.operations.GetLiveStreamByIdResponse;
import org.openapis.openapi.models.operations.UpdateLiveStreamRequest;
import org.openapis.openapi.models.operations.UpdateLiveStreamRequestBuilder;
import org.openapis.openapi.models.operations.UpdateLiveStreamResponse;
import org.openapis.openapi.operations.GetLiveStreamById;
import org.openapis.openapi.operations.UpdateLiveStream;
import org.openapis.openapi.utils.Headers;
import org.openapis.openapi.utils.Options;


public class ManageLiveStream {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncManageLiveStream asyncSDK;

    ManageLiveStream(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncManageLiveStream(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncManageLiveStream async() {
        return asyncSDK;
    }

    /**
     * Get stream by ID
     * 
     * <p>This endpoint retrieves details about a specific live stream by its unique `streamId`. It includes
     * data such as the stream’s `status` (idle, preparing, active, disabled), `metadata` (title,
     * description), and more.
     * #### Example
     * 
     * <p>Suppose a news agency is broadcasting a live event and wants to track the configurations set for the
     * live stream while also checking the stream's status.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @return The call builder
     */
    public GetLiveStreamByIdRequestBuilder get() {
        return new GetLiveStreamByIdRequestBuilder(sdkConfiguration);
    }

    /**
     * Get stream by ID
     * 
     * <p>This endpoint retrieves details about a specific live stream by its unique `streamId`. It includes
     * data such as the stream’s `status` (idle, preparing, active, disabled), `metadata` (title,
     * description), and more.
     * #### Example
     * 
     * <p>Suppose a news agency is broadcasting a live event and wants to track the configurations set for the
     * live stream while also checking the stream's status.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @param streamId Upon creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetLiveStreamByIdResponse get(@Nonnull String streamId) {
        return get(streamId, null);
    }

    /**
     * Get stream by ID
     * 
     * <p>This endpoint retrieves details about a specific live stream by its unique `streamId`. It includes
     * data such as the stream’s `status` (idle, preparing, active, disabled), `metadata` (title,
     * description), and more.
     * #### Example
     * 
     * <p>Suppose a news agency is broadcasting a live event and wants to track the configurations set for the
     * live stream while also checking the stream's status.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @param streamId Upon creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetLiveStreamByIdResponse get(@Nonnull String streamId, @Nullable Options options) {
        GetLiveStreamByIdRequest request = new GetLiveStreamByIdRequest(streamId);
        RequestOperation<GetLiveStreamByIdRequest, GetLiveStreamByIdResponse> operation
              = new GetLiveStreamById.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Update a stream
     * 
     * <p>This endpoint allows you to modify the parameters of an existing live stream, such as its `metadata`
     * (title, description) or the `reconnectWindow`. It’s useful for making changes to a stream that has
     * already been created but not yet ended. After the live stream is disabled, you cannot update a
     * stream.
     * 
     * <p>The updated stream parameters and the `streamId` needs to be shared in the request, and FastPix
     * returns the updated stream details. After the update, <a
     * href="https://docs.fastpix.io/docs/live-events#videolive_streamupdated">video.live_stream.updated</a>
     * webhook event notifies your system.
     * 
     * <p>#### Example
     * 
     * <p>A host realizes they need to extend the reconnect window for their live stream in case they lose
     * connection temporarily during the event. Or suppose during a multi-day online conference, the event
     * organizers need to update the stream title to reflect the next day"s session while keeping the same
     * stream ID for continuity.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @return The call builder
     */
    public UpdateLiveStreamRequestBuilder update() {
        return new UpdateLiveStreamRequestBuilder(sdkConfiguration);
    }

    /**
     * Update a stream
     * 
     * <p>This endpoint allows you to modify the parameters of an existing live stream, such as its `metadata`
     * (title, description) or the `reconnectWindow`. It’s useful for making changes to a stream that has
     * already been created but not yet ended. After the live stream is disabled, you cannot update a
     * stream.
     * 
     * <p>The updated stream parameters and the `streamId` needs to be shared in the request, and FastPix
     * returns the updated stream details. After the update, <a
     * href="https://docs.fastpix.io/docs/live-events#videolive_streamupdated">video.live_stream.updated</a>
     * webhook event notifies your system.
     * 
     * <p>#### Example
     * 
     * <p>A host realizes they need to extend the reconnect window for their live stream in case they lose
     * connection temporarily during the event. Or suppose during a multi-day online conference, the event
     * organizers need to update the stream title to reflect the next day"s session while keeping the same
     * stream ID for continuity.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @param streamId After creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @param body 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateLiveStreamResponse update(@Nonnull String streamId, @Nonnull PatchLiveStreamRequest body) {
        return update(streamId, body, null);
    }

    /**
     * Update a stream
     * 
     * <p>This endpoint allows you to modify the parameters of an existing live stream, such as its `metadata`
     * (title, description) or the `reconnectWindow`. It’s useful for making changes to a stream that has
     * already been created but not yet ended. After the live stream is disabled, you cannot update a
     * stream.
     * 
     * <p>The updated stream parameters and the `streamId` needs to be shared in the request, and FastPix
     * returns the updated stream details. After the update, <a
     * href="https://docs.fastpix.io/docs/live-events#videolive_streamupdated">video.live_stream.updated</a>
     * webhook event notifies your system.
     * 
     * <p>#### Example
     * 
     * <p>A host realizes they need to extend the reconnect window for their live stream in case they lose
     * connection temporarily during the event. Or suppose during a multi-day online conference, the event
     * organizers need to update the stream title to reflect the next day"s session while keeping the same
     * stream ID for continuity.
     * 
     * <p>Related guide: <a href="https://docs.fastpix.io/docs/manage-streams">Manage streams</a>
     * 
     * @param streamId After creating a new live stream, FastPix assigns a unique identifier to the stream.
     * @param body 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateLiveStreamResponse update(
            @Nonnull String streamId, @Nonnull PatchLiveStreamRequest body,
            @Nullable Options options) {
        UpdateLiveStreamRequest request = new UpdateLiveStreamRequest(streamId, body);
        RequestOperation<UpdateLiveStreamRequest, UpdateLiveStreamResponse> operation
              = new UpdateLiveStream.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
