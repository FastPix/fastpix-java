/* 
 * This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */
package io.fastpix.sdk;

import static io.fastpix.sdk.operations.Operations.RequestOperation;

import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;
import io.fastpix.sdk.models.operations.CreateMediaPlaybackIdRequest;
import io.fastpix.sdk.models.operations.CreateMediaPlaybackIdRequestBody;
import io.fastpix.sdk.models.operations.CreateMediaPlaybackIdRequestBuilder;
import io.fastpix.sdk.models.operations.CreateMediaPlaybackIdResponse;
import io.fastpix.sdk.models.operations.DeleteMediaPlaybackIdRequest;
import io.fastpix.sdk.models.operations.DeleteMediaPlaybackIdRequestBuilder;
import io.fastpix.sdk.models.operations.DeleteMediaPlaybackIdResponse;
import io.fastpix.sdk.models.operations.GetPlaybackIdRequest;
import io.fastpix.sdk.models.operations.GetPlaybackIdRequestBuilder;
import io.fastpix.sdk.models.operations.GetPlaybackIdResponse;
import io.fastpix.sdk.models.operations.ListPlaybackIdsRequest;
import io.fastpix.sdk.models.operations.ListPlaybackIdsRequestBuilder;
import io.fastpix.sdk.models.operations.ListPlaybackIdsResponse;
import io.fastpix.sdk.models.operations.UpdateDomainRestrictionsRequest;
import io.fastpix.sdk.models.operations.UpdateDomainRestrictionsRequestBody;
import io.fastpix.sdk.models.operations.UpdateDomainRestrictionsRequestBuilder;
import io.fastpix.sdk.models.operations.UpdateDomainRestrictionsResponse;
import io.fastpix.sdk.models.operations.UpdateUserAgentRestrictionsRequest;
import io.fastpix.sdk.models.operations.UpdateUserAgentRestrictionsRequestBody;
import io.fastpix.sdk.models.operations.UpdateUserAgentRestrictionsRequestBuilder;
import io.fastpix.sdk.models.operations.UpdateUserAgentRestrictionsResponse;
import io.fastpix.sdk.operations.CreateMediaPlaybackId;
import io.fastpix.sdk.operations.DeleteMediaPlaybackId;
import io.fastpix.sdk.operations.GetPlaybackId;
import io.fastpix.sdk.operations.ListPlaybackIds;
import io.fastpix.sdk.operations.UpdateDomainRestrictions;
import io.fastpix.sdk.operations.UpdateUserAgentRestrictions;
import io.fastpix.sdk.utils.Headers;
import io.fastpix.sdk.utils.Options;

/**
 * Operations for video playback management
 */
public class Playback {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncPlayback asyncSDK;

    Playback(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncPlayback(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncPlayback async() {
        return asyncSDK;
    }

    /**
     * Create a playback ID
     * 
     * <p>You can create a new playback ID for a specific media asset. If you have already retrieved an
     * existing `playbackId` using the <a href="https://docs.fastpix.io/reference/get-media">Get Media by
     * ID</a> endpoint for a media asset, you can use this endpoint to generate a new playback ID with a
     * specified access policy.
     * 
     * <p>If you want to create a private playback ID for a media asset that already has a public playback ID,
     * this endpoint also allows you to do so by specifying the desired access policy.
     * 
     * <p>#### How it works
     * 
     * <p>1. Make a `POST` request to this endpoint, replacing `&lt;mediaId&gt;` with the `uploadId` or `id`
     * of the media asset.
     * 
     * <p>2. Include the `accessPolicy` in the request body with `private` or `public` as the value.
     * 
     * <p>3. You receive a response containing the newly created playback ID with the specified access level.
     * 
     * <p>#### Example
     * A video streaming service generates playback IDs for each media file when users request to view
     * specific content. The video player then uses the playback ID to stream the video.
     * 
     * @return The call builder
     */
    public CreateMediaPlaybackIdRequestBuilder createId() {
        return new CreateMediaPlaybackIdRequestBuilder(sdkConfiguration);
    }

    /**
     * Create a playback ID
     * 
     * <p>You can create a new playback ID for a specific media asset. If you have already retrieved an
     * existing `playbackId` using the <a href="https://docs.fastpix.io/reference/get-media">Get Media by
     * ID</a> endpoint for a media asset, you can use this endpoint to generate a new playback ID with a
     * specified access policy.
     * 
     * <p>If you want to create a private playback ID for a media asset that already has a public playback ID,
     * this endpoint also allows you to do so by specifying the desired access policy.
     * 
     * <p>#### How it works
     * 
     * <p>1. Make a `POST` request to this endpoint, replacing `&lt;mediaId&gt;` with the `uploadId` or `id`
     * of the media asset.
     * 
     * <p>2. Include the `accessPolicy` in the request body with `private` or `public` as the value.
     * 
     * <p>3. You receive a response containing the newly created playback ID with the specified access level.
     * 
     * <p>#### Example
     * A video streaming service generates playback IDs for each media file when users request to view
     * specific content. The video player then uses the playback ID to stream the video.
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CreateMediaPlaybackIdResponse createId(@Nonnull String mediaId) {
        return createId(mediaId, null, null);
    }

    /**
     * Create a playback ID
     * 
     * <p>You can create a new playback ID for a specific media asset. If you have already retrieved an
     * existing `playbackId` using the <a href="https://docs.fastpix.io/reference/get-media">Get Media by
     * ID</a> endpoint for a media asset, you can use this endpoint to generate a new playback ID with a
     * specified access policy.
     * 
     * <p>If you want to create a private playback ID for a media asset that already has a public playback ID,
     * this endpoint also allows you to do so by specifying the desired access policy.
     * 
     * <p>#### How it works
     * 
     * <p>1. Make a `POST` request to this endpoint, replacing `&lt;mediaId&gt;` with the `uploadId` or `id`
     * of the media asset.
     * 
     * <p>2. Include the `accessPolicy` in the request body with `private` or `public` as the value.
     * 
     * <p>3. You receive a response containing the newly created playback ID with the specified access level.
     * 
     * <p>#### Example
     * A video streaming service generates playback IDs for each media file when users request to view
     * specific content. The video player then uses the playback ID to stream the video.
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @param body Request body for creating playback id for an media
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CreateMediaPlaybackIdResponse createId(
            @Nonnull String mediaId, @Nullable CreateMediaPlaybackIdRequestBody body,
            @Nullable Options options) {
        CreateMediaPlaybackIdRequest request = new CreateMediaPlaybackIdRequest(mediaId, body);
        RequestOperation<CreateMediaPlaybackIdRequest, CreateMediaPlaybackIdResponse> operation
              = new CreateMediaPlaybackId.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Get all playback IDs details for a media
     * 
     * <p>Retrieves all playback IDs associated with a given media asset, including each playback ID’s access
     * policy and detailed access restrictions such as allowed or denied domains and user agents.
     * 
     * <p>**How it works:**
     * 1. Send a `GET` request to this endpoint with the target `mediaId`.
     * 2. The response includes an array of playback ID records with their respective access controls.
     * 
     * <p>**Use case:**
     * Useful for validating and managing playback permissions programmatically, reviewing restriction
     * settings, or powering an access control dashboard.
     * 
     * @return The call builder
     */
    public ListPlaybackIdsRequestBuilder list() {
        return new ListPlaybackIdsRequestBuilder(sdkConfiguration);
    }

    /**
     * Get all playback IDs details for a media
     * 
     * <p>Retrieves all playback IDs associated with a given media asset, including each playback ID’s access
     * policy and detailed access restrictions such as allowed or denied domains and user agents.
     * 
     * <p>**How it works:**
     * 1. Send a `GET` request to this endpoint with the target `mediaId`.
     * 2. The response includes an array of playback ID records with their respective access controls.
     * 
     * <p>**Use case:**
     * Useful for validating and managing playback permissions programmatically, reviewing restriction
     * settings, or powering an access control dashboard.
     * 
     * @param mediaId 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListPlaybackIdsResponse list(@Nonnull String mediaId) {
        return list(mediaId, null);
    }

    /**
     * Get all playback IDs details for a media
     * 
     * <p>Retrieves all playback IDs associated with a given media asset, including each playback ID’s access
     * policy and detailed access restrictions such as allowed or denied domains and user agents.
     * 
     * <p>**How it works:**
     * 1. Send a `GET` request to this endpoint with the target `mediaId`.
     * 2. The response includes an array of playback ID records with their respective access controls.
     * 
     * <p>**Use case:**
     * Useful for validating and managing playback permissions programmatically, reviewing restriction
     * settings, or powering an access control dashboard.
     * 
     * @param mediaId 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListPlaybackIdsResponse list(@Nonnull String mediaId, @Nullable Options options) {
        ListPlaybackIdsRequest request = new ListPlaybackIdsRequest(mediaId);
        RequestOperation<ListPlaybackIdsRequest, ListPlaybackIdsResponse> operation
              = new ListPlaybackIds.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Delete a playback ID
     * 
     * <p>This endpoint deletes a specific playback ID associated with a media asset. Deleting a `playback ID`
     * revokes access to the media content linked to that ID.
     * 
     * <p>#### How it works
     * 
     * <p>1. Make a `DELETE` request to this endpoint, replacing `&lt;mediaId&gt;` with the unique ID of the
     * media asset from which you want to delete the playback ID.
     * 
     * <p>2. Include the `playbackId` you want to delete in the request body.
     * 
     * <p>#### Example
     * 
     * <p>Your platform offers limited-time access to premium content. When the subscription expires, you can
     * revoke access to the content by deleting the associated playback ID, preventing users from streaming
     * the video further.
     * 
     * @return The call builder
     */
    public DeleteMediaPlaybackIdRequestBuilder deleteId() {
        return new DeleteMediaPlaybackIdRequestBuilder(sdkConfiguration);
    }

    /**
     * Delete a playback ID
     * 
     * <p>This endpoint deletes a specific playback ID associated with a media asset. Deleting a `playback ID`
     * revokes access to the media content linked to that ID.
     * 
     * <p>#### How it works
     * 
     * <p>1. Make a `DELETE` request to this endpoint, replacing `&lt;mediaId&gt;` with the unique ID of the
     * media asset from which you want to delete the playback ID.
     * 
     * <p>2. Include the `playbackId` you want to delete in the request body.
     * 
     * <p>#### Example
     * 
     * <p>Your platform offers limited-time access to premium content. When the subscription expires, you can
     * revoke access to the content by deleting the associated playback ID, preventing users from streaming
     * the video further.
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @param playbackId Return the universal unique identifier for playbacks  which can contain a maximum of 255 characters. 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DeleteMediaPlaybackIdResponse deleteId(@Nonnull String mediaId, @Nonnull String playbackId) {
        return deleteId(mediaId, playbackId, null);
    }

    /**
     * Delete a playback ID
     * 
     * <p>This endpoint deletes a specific playback ID associated with a media asset. Deleting a `playback ID`
     * revokes access to the media content linked to that ID.
     * 
     * <p>#### How it works
     * 
     * <p>1. Make a `DELETE` request to this endpoint, replacing `&lt;mediaId&gt;` with the unique ID of the
     * media asset from which you want to delete the playback ID.
     * 
     * <p>2. Include the `playbackId` you want to delete in the request body.
     * 
     * <p>#### Example
     * 
     * <p>Your platform offers limited-time access to premium content. When the subscription expires, you can
     * revoke access to the content by deleting the associated playback ID, preventing users from streaming
     * the video further.
     * 
     * @param mediaId The unique identifier assigned to the media when created. The value must be a valid UUID.
     * @param playbackId Return the universal unique identifier for playbacks  which can contain a maximum of 255 characters. 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DeleteMediaPlaybackIdResponse deleteId(
            @Nonnull String mediaId, @Nonnull String playbackId,
            @Nullable Options options) {
        DeleteMediaPlaybackIdRequest request = new DeleteMediaPlaybackIdRequest(mediaId, playbackId);
        RequestOperation<DeleteMediaPlaybackIdRequest, DeleteMediaPlaybackIdResponse> operation
              = new DeleteMediaPlaybackId.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Get a playback ID
     * 
     * <p>This endpoint retrieves details about a specific playback ID associated with a media asset. Use it
     * to check the access policy for that specific playback ID, such as whether it is public or private.
     * 
     * <p>**How it works:**
     * 1. Make a GET request to the endpoint, replacing `{mediaId}` with the media ID and `{playbackId}`
     * with the playback ID.
     * 2. This request is useful for auditing or validation before granting playback access in your
     * application.
     * 
     * <p>**Example:**
     * A media platform might use this endpoint to verify if a playback ID is public or private before
     * embedding the video in a frontend player or allowing access to a restricted group.
     * 
     * @return The call builder
     */
    public GetPlaybackIdRequestBuilder get() {
        return new GetPlaybackIdRequestBuilder(sdkConfiguration);
    }

    /**
     * Get a playback ID
     * 
     * <p>This endpoint retrieves details about a specific playback ID associated with a media asset. Use it
     * to check the access policy for that specific playback ID, such as whether it is public or private.
     * 
     * <p>**How it works:**
     * 1. Make a GET request to the endpoint, replacing `{mediaId}` with the media ID and `{playbackId}`
     * with the playback ID.
     * 2. This request is useful for auditing or validation before granting playback access in your
     * application.
     * 
     * <p>**Example:**
     * A media platform might use this endpoint to verify if a playback ID is public or private before
     * embedding the video in a frontend player or allowing access to a restricted group.
     * 
     * @param mediaId 
     * @param playbackId 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetPlaybackIdResponse get(@Nonnull String mediaId, @Nonnull String playbackId) {
        return get(mediaId, playbackId, null);
    }

    /**
     * Get a playback ID
     * 
     * <p>This endpoint retrieves details about a specific playback ID associated with a media asset. Use it
     * to check the access policy for that specific playback ID, such as whether it is public or private.
     * 
     * <p>**How it works:**
     * 1. Make a GET request to the endpoint, replacing `{mediaId}` with the media ID and `{playbackId}`
     * with the playback ID.
     * 2. This request is useful for auditing or validation before granting playback access in your
     * application.
     * 
     * <p>**Example:**
     * A media platform might use this endpoint to verify if a playback ID is public or private before
     * embedding the video in a frontend player or allowing access to a restricted group.
     * 
     * @param mediaId 
     * @param playbackId 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetPlaybackIdResponse get(
            @Nonnull String mediaId, @Nonnull String playbackId,
            @Nullable Options options) {
        GetPlaybackIdRequest request = new GetPlaybackIdRequest(mediaId, playbackId);
        RequestOperation<GetPlaybackIdRequest, GetPlaybackIdResponse> operation
              = new GetPlaybackId.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Update domain restrictions for a playback ID
     * 
     * <p>This endpoint updates domain-level restrictions for a specific playback ID associated with a media
     * asset.
     * It allows you to restrict playback to specific domains or block known unauthorized domains.
     * 
     * <p>**How it works:**
     * 1. Make a `PATCH` request to this endpoint with your desired domain access configuration.
     * 2. Set a default policy (`allow` or `deny`) and specify domain names in the `allow` or `deny` lists.
     * 3. This is commonly used to restrict video playback to your website or approved client domains.
     * 
     * <p>**Example:**
     * A streaming service can allow playback only from `example.com` and deny all others by setting:
     * `"defaultPolicy": "deny"` and `"allow": ["example.com"]`.
     * 
     * @return The call builder
     */
    public UpdateDomainRestrictionsRequestBuilder updateDomainRestrictions() {
        return new UpdateDomainRestrictionsRequestBuilder(sdkConfiguration);
    }

    /**
     * Update domain restrictions for a playback ID
     * 
     * <p>This endpoint updates domain-level restrictions for a specific playback ID associated with a media
     * asset.
     * It allows you to restrict playback to specific domains or block known unauthorized domains.
     * 
     * <p>**How it works:**
     * 1. Make a `PATCH` request to this endpoint with your desired domain access configuration.
     * 2. Set a default policy (`allow` or `deny`) and specify domain names in the `allow` or `deny` lists.
     * 3. This is commonly used to restrict video playback to your website or approved client domains.
     * 
     * <p>**Example:**
     * A streaming service can allow playback only from `example.com` and deny all others by setting:
     * `"defaultPolicy": "deny"` and `"allow": ["example.com"]`.
     * 
     * @param mediaId 
     * @param playbackId 
     * @param body 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateDomainRestrictionsResponse updateDomainRestrictions(
            @Nonnull String mediaId, @Nonnull String playbackId,
            @Nonnull UpdateDomainRestrictionsRequestBody body) {
        return updateDomainRestrictions(mediaId, playbackId, body,
            null);
    }

    /**
     * Update domain restrictions for a playback ID
     * 
     * <p>This endpoint updates domain-level restrictions for a specific playback ID associated with a media
     * asset.
     * It allows you to restrict playback to specific domains or block known unauthorized domains.
     * 
     * <p>**How it works:**
     * 1. Make a `PATCH` request to this endpoint with your desired domain access configuration.
     * 2. Set a default policy (`allow` or `deny`) and specify domain names in the `allow` or `deny` lists.
     * 3. This is commonly used to restrict video playback to your website or approved client domains.
     * 
     * <p>**Example:**
     * A streaming service can allow playback only from `example.com` and deny all others by setting:
     * `"defaultPolicy": "deny"` and `"allow": ["example.com"]`.
     * 
     * @param mediaId 
     * @param playbackId 
     * @param body 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateDomainRestrictionsResponse updateDomainRestrictions(
            @Nonnull String mediaId, @Nonnull String playbackId,
            @Nonnull UpdateDomainRestrictionsRequestBody body, @Nullable Options options) {
        UpdateDomainRestrictionsRequest request = new UpdateDomainRestrictionsRequest(mediaId, playbackId, body);
        RequestOperation<UpdateDomainRestrictionsRequest, UpdateDomainRestrictionsResponse> operation
              = new UpdateDomainRestrictions.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Update user-agent restrictions for a playback ID
     * 
     * <p>This endpoint allows updating user-agent restrictions for a specific playback ID associated with a
     * media asset.
     * It can be used to allow or deny specific user-agents during playback request evaluation.
     * 
     * <p>**How it works:**
     * 1. Make a `PATCH` request to this endpoint with your desired user-agent access configuration.
     * 2. Specify a default policy (`allow` or `deny`) and provide specific `allow` or `deny` lists.
     * 3. Use this to restrict access to specific browsers, devices, or bots.
     * 
     * <p>**Example:**
     * A developer may configure a playback ID to deny access from known scraping user-agents while
     * allowing all others by default.
     * 
     * @return The call builder
     */
    public UpdateUserAgentRestrictionsRequestBuilder updateUserAgentRestrictions() {
        return new UpdateUserAgentRestrictionsRequestBuilder(sdkConfiguration);
    }

    /**
     * Update user-agent restrictions for a playback ID
     * 
     * <p>This endpoint allows updating user-agent restrictions for a specific playback ID associated with a
     * media asset.
     * It can be used to allow or deny specific user-agents during playback request evaluation.
     * 
     * <p>**How it works:**
     * 1. Make a `PATCH` request to this endpoint with your desired user-agent access configuration.
     * 2. Specify a default policy (`allow` or `deny`) and provide specific `allow` or `deny` lists.
     * 3. Use this to restrict access to specific browsers, devices, or bots.
     * 
     * <p>**Example:**
     * A developer may configure a playback ID to deny access from known scraping user-agents while
     * allowing all others by default.
     * 
     * @param mediaId 
     * @param playbackId 
     * @param body 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateUserAgentRestrictionsResponse updateUserAgentRestrictions(
            @Nonnull String mediaId, @Nonnull String playbackId,
            @Nonnull UpdateUserAgentRestrictionsRequestBody body) {
        return updateUserAgentRestrictions(mediaId, playbackId, body,
            null);
    }

    /**
     * Update user-agent restrictions for a playback ID
     * 
     * <p>This endpoint allows updating user-agent restrictions for a specific playback ID associated with a
     * media asset.
     * It can be used to allow or deny specific user-agents during playback request evaluation.
     * 
     * <p>**How it works:**
     * 1. Make a `PATCH` request to this endpoint with your desired user-agent access configuration.
     * 2. Specify a default policy (`allow` or `deny`) and provide specific `allow` or `deny` lists.
     * 3. Use this to restrict access to specific browsers, devices, or bots.
     * 
     * <p>**Example:**
     * A developer may configure a playback ID to deny access from known scraping user-agents while
     * allowing all others by default.
     * 
     * @param mediaId 
     * @param playbackId 
     * @param body 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateUserAgentRestrictionsResponse updateUserAgentRestrictions(
            @Nonnull String mediaId, @Nonnull String playbackId,
            @Nonnull UpdateUserAgentRestrictionsRequestBody body, @Nullable Options options) {
        UpdateUserAgentRestrictionsRequest request = new UpdateUserAgentRestrictionsRequest(mediaId, playbackId, body);
        RequestOperation<UpdateUserAgentRestrictionsRequest, UpdateUserAgentRestrictionsResponse> operation
              = new UpdateUserAgentRestrictions.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
